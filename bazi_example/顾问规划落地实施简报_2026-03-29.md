# 顾问规划落地简报（2026-03-29）

文档对照：`bazi_example/顾问Agent玄学算法修改规划_2026-03-28.md`

## 一、整体结论
- 当前已完成“算法2.0工程化落地”主线，核心能力可用于持续迭代。
- 完成度按执行清单计：16/16（100%）。
- 完成度按可运行产品能力计：约92%（剩余为高精天文内核全面替换与真人行为校准）。

## 二、已落地模块（对应顾问规划主要求）
1. 统一契约与解释结构
- `match_reasons.modules` 与 `match_reasons.module_explanations` 统一输出。
- 增加 `priority/priority_level/priority_reason/priority_rank`。
- 增加 `evidence_strength/evidence_strength_reason`。
- 增加 `core_tags/aux_tags`、`core_tag_explains/aux_tag_explains`。
- 增加 `core_tag_refs/aux_tag_refs`（理论依据来源）。

2. 匹配解释接口独立化
- 已支持：`/api/v1/matches/{targetUserId}/explanation`
- 兼容保留：`/api/v1/match/{targetUserId}/explanation`

3. 证据标签标准化
- 新增并维护 `config/match_evidence_tags.php`，标签可读化并附说明/理论依据。

4. 八字/属相/星盘工程化改造
- 八字特征层、属相派生统一、星盘引擎适配抽象、pair-chart 特征拆分已落地。
- MBTI 题库版本化与兼容读取已落地。

5. 校准与门禁
- display_score / rank_score 分离已落地。
- 离线评估命令与 release gate 回归接入已完成。

## 三、前端（Flutter）本轮新增交互能力
1. 模块解释可用性增强
- 证据强度分布 + 关注分布双筛选。
- 当前筛选摘要条（含一键清空与反馈）。
- 风险/证据/关注/置信等状态芯片自动换行，避免溢出。

2. 证据可追溯增强
- 标签点击弹窗展示“解释 + 理论依据”。
- 新增“理论依据”聚合卡片（按优先级展示）。

3. 弱证据模块快速定位
- 弱证据芯片点击后：自动切换筛选 -> 滚动定位 -> 短暂高亮。
- 未命中目标模块时，给出明确提示，不再无反馈。

## 四、质量与测试状态
- 后端：`MatchApiTest` + `MatchPayloadContractTest` 通过。
- 前端：`flutter analyze lib/features/match` 通过。
- 契约文档已同步：`bazi_example/匹配解释接口契约_v2_2026-03-29.md`。

## 五、尚未完成（按顾问规划高阶项）
1. 生产级高精天文引擎全链路替换（当前为适配抽象 + 可切换实现）。
2. 真人行为数据驱动的排序校准（Beta前置条件不足，暂未进入）。
3. 更深层玄学规则（如更完整十神/用忌细分与专业派别参数化）仍可在2.1继续扩展。

## 六、建议下一步（2.1）
1. 上线前先做“高精引擎灰度开关 + 对拍报告”。
2. 补“解释可信度阈值告警”（低置信时前端明确提示仅供参考）。
3. 在小规模真实样本可得后，启动 rank_score 校准。
