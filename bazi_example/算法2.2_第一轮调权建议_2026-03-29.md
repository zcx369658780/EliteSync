# 算法2.2 第一轮小步调权建议（2026-03-29）

目标：在已有非零标签样本基础上，进行第一轮“最多2项、每项不超过10%”的保守调权，优先提升 `reply_24h`，同时观察 `sustained_7d` 不恶化。

## 1) 当前基线

来自 `docs/devlogs/PAIR_OUTCOME_METRICS.json`：
- total_pairs: 481
- mutual_like_rate_pct: 12.68
- first_message_rate_pct: 12.27
- reply_24h_rate_pct: 2.91
- sustained_7d_rate_pct: 0.62

来自 `docs/devlogs/MATCH_CALIBRATION_DATASET.csv`：
- reply24h 正样本: 14 / 481 (2.91%)
- sustained7d 正样本: 3 / 481 (0.62%)

## 2) 建议调权（第一轮）

仅调整 core_weights 两项：

1. `personality`: `0.58 -> 0.61`（+5.17%）
2. `astro`: `0.35 -> 0.32`（-8.57%）

`mbti` 维持 `0.07` 不变。

说明：
- 两项都满足单次变更 <=10%。
- personality 更靠近沟通与互动早期信号，理论上更利于提升 `first_message/reply_24h`。
- astro 暂降，降低在西占 canonical 尚未全量时对排序的影响。

## 3) .env 设置（建议值）

```env
MATCH_WEIGHT_PERSONALITY=0.61
MATCH_WEIGHT_MBTI=0.07
MATCH_WEIGHT_ASTRO=0.32
```

应用：
```bash
php artisan config:cache
```

## 4) 回滚值（上一版）

```env
MATCH_WEIGHT_PERSONALITY=0.58
MATCH_WEIGHT_MBTI=0.07
MATCH_WEIGHT_ASTRO=0.35
```

## 5) 观察窗口与止损

观察窗口：3-7天。

核心观察：
- `reply_24h_rate_pct`（目标上升）
- `sustained_7d_rate_pct`（不显著下降）

止损条件（任一满足即回滚）：
- `reply_24h_rate_pct` 下降超过 20%
- `sustained_7d_rate_pct` 下降超过 20%
- 高置信模块解释与实际互动明显背离（人工抽样）

## 6) 下一轮入口

按周执行：
1. `app:dev:pair-outcome-metrics`
2. `app:dev:export-match-calibration`
3. 若指标改善且稳定，再做第二轮（最多2项，<=10%）。
