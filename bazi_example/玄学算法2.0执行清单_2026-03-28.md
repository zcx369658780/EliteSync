# 玄学算法 2.0 执行清单（2026-03-28）

> 基于《玄学算法2.0总方案_2026-03-28.md》拆解
> 当前状态：待执行（全部任务默认 `TODO`）

---

## 一、执行节奏（建议）

1. 先做 Phase A（计算正确性）
2. 再做 Phase B（特征和解释增强）
3. 最后做 Phase C（离线校准与发布门禁）

---

## 二、Phase A（P0）计算正确性与统一契约

## A1. 统一算法输出契约（八字/属相/星座/星盘/合盘/MBTI）
- 状态：DONE（2026-03-28，本地已落地契约测试）
- 优先级：P0
- 目标：所有模块统一输出字段：`score/confidence/reason_short/reason_detail/risk_detail/evidence_tags/evidence/degraded/degrade_reason`
- 交付物：
  - 后端 DTO/响应字段清单
  - 契约测试（Feature Test）
- 验收：新增或修改模块不再出现字段缺失/命名漂移

## A2. 八字口径统一（删除“文本相似度主逻辑”地位）
- 状态：DONE（2026-03-28，五行结构法为主，文本相似仅兜底）
- 优先级：P0
- 目标：五行结构法为主，文本相似仅兜底
- 交付物：
  - `AstroCompatibilityService::scoreBazi` 重构
  - 配置项版本化（v2 参数）
- 验收：
  - 完整五行输入场景不再走相似度路径
  - 缺失场景降级标记准确

## A3. 属相来源统一为八字年柱派生
- 状态：DONE（2026-03-28，已接入年柱派生链路并切到八字优先）
- 优先级：P0
- 目标：属相只从标准历法/年柱链路派生，不再独立公历简算
- 交付物：
  - `ChineseZodiacService` 与八字链路对齐
  - 数据回填脚本（已有用户）
- 验收：立春/春节边界样本结果一致

## A4. 星盘计算标准化接入抽象层
- 状态：DONE（2026-03-28，已新增 AstroEngineAdapter 抽象与双实现切换）
- 优先级：P0
- 目标：新增 `AstroEngineAdapter` 抽象，支持“现实现 + 新标准引擎”双实现切换
- 交付物：
  - Adapter 接口
  - 当前实现封装为 DefaultAdapter
  - 新引擎占位 Adapter（可返回同结构）
- 验收：业务层不直接依赖某一算法实现

## A5. 边界样本回归集
- 状态：DONE（2026-03-28，已新增 test vectors + 单元测试）
- 优先级：P0
- 目标：建立八字/属相/时区/时辰边界测试样本库
- 交付物：
  - `bazi_example/test_vectors/*.json`
  - 自动测试命令脚本（php artisan test + custom checker）
- 验收：边界样本通过率 >= 99%

## A6. 开源依赖复核台账（可用性+许可证+商用）
- 状态：DONE（2026-03-28，`OpenSource_Review_2026-03-28.md`）
- 优先级：P0
- 目标：对顾问提到的库逐个复核并留痕
- 交付物：
  - `bazi_example/OpenSource_Review_2026-03-28.md`
  - 每个依赖状态：`approved/conditional/unverified`
- 验收：未复核项不得进入正式依赖清单

---

## 三、Phase B（P1）特征工程与解释增强

## B1. 八字特征工程扩展（结构/关系/时间）
- 状态：DONE（2026-03-28，已落地 BaziFeatureExtractor 并接入主评分）
- 优先级：P1
- 目标：新增中间特征层对象（而非直接输出分数）
- 交付物：
  - `BaziFeatureExtractor`（可先在 Laravel service 内）
  - 特征字段文档
- 验收：评分器只读特征，不直接读原始字符串

## B2. 合盘特征扩展（Synastry/Composite 预留）
- 状态：DONE（2026-03-28，pair_chart 已拆分 features 并与解释可追溯）
- 优先级：P1
- 目标：现有 `pair_chart` 拆成可扩展分项
- 交付物：
  - `pair_chart.features` 结构化输出
  - 模块解释模板与 evidence 一一对应
- 验收：解释文本可追溯到特征项

## B3. MBTI 题库版本化
- 状态：DONE（2026-03-28，后端已支持 versioned quiz sets：lite3_v1/lite5_v1）
- 优先级：P1
- 目标：从 `lite3_v1` 扩展到“版本化题库 + 维度置信区间”
- 交付物：
  - `mbti_question_sets` 配置/表结构
  - 新版提交与结果接口兼容旧版
- 验收：v1/v2 可并存，匹配引擎读取 canonical 字段不变

## B4. 风险标签标准化
- 状态：DONE（2026-03-28，已新增 evidence tag 字典并输出可读标签）
- 优先级：P1
- 目标：统一 `evidence_tags` 到用户可读标签映射
- 交付物：
  - tag 字典文件
  - 前端映射统一
- 验收：前端不再直出技术变量名

## B5. 匹配解释独立接口（按目标用户）
- 状态：DONE（2026-03-28，已新增 `/api/v1/matches/{targetUserId}/explanation`，并保留 `/api/v1/match/{targetUserId}/explanation` 兼容）
- 优先级：P1
- 目标：前端可按目标用户拉取完整解释，不依赖“当前匹配”唯一入口
- 交付物：
  - 路由与控制器接口
  - Feature Test 覆盖“命中/未命中/旧路径兼容”
- 验收：接口稳定返回 `match_reasons/modules/evidence_tags/evidence_tag_labels`

## B6. 结构化模块解释字段（前后端直连）
- 状态：DONE（2026-03-28，后端新增 `match_reasons.module_explanations`，Flutter 详情页已优先直渲染）
- 优先级：P1
- 目标：前端不再依赖字符串解析模块解释，降低文案改动导致的解析风险
- 交付物：
  - 后端统一返回 `[{label,score,reason,risk,tags}]`
  - Flutter DTO/Entity/Mapper 扩展并接入 UI 渲染
- 验收：模块解释字段改动不再导致前端显示错位

## B7. 解释优先级排序保障（契约测试）
- 状态：DONE（2026-03-29，已新增 explanation 接口 priority 排序测试）
- 优先级：P1
- 目标：确保 `module_explanations` 即使输入乱序，也按 priority 高到低稳定输出
- 交付物：
  - `MatchApiTest::test_explanation_by_target_sorts_module_explanations_by_priority_desc`
- 验收：后端回归测试可阻断排序回归

---

## 四、Phase C（P1-P2）离线校准与发布门禁

## C1. display_score / rank_score 分离
- 状态：DONE（2026-03-28，已在 match_reasons 与 API 输出分离字段）
- 优先级：P1
- 目标：用户展示分与系统排序分分离
- 交付物：
  - 数据模型字段扩展
  - API 输出约定
- 验收：兼容旧接口，前端展示不受影响

## C2. 离线评估任务管线
- 状态：DONE（2026-03-28，已新增 `app:eval:astro-v2` 离线评估命令）
- 优先级：P1
- 目标：每次算法改动自动生成离线评估报告
- 交付物：
  - `scripts/eval_astro_v2.*`
  - 报告模板（分数分布、模块贡献、降级占比）
- 验收：PR 前可一键执行

## C3. 发布门禁加入算法回归
- 状态：DONE（2026-03-28，release gate 已加入 Astro 回归测试步骤）
- 优先级：P2
- 目标：Regression 增加算法一致性检查
- 交付物：
  - CI 步骤：算法契约测试 + 边界样本测试
- 验收：门禁失败可阻断合并

---

## 五、立即开工清单（本周）

## Sprint-1（建议先做）
1. A1 统一输出契约
2. A2 八字主逻辑重构
3. A3 属相派生统一
4. A5 边界样本与回归脚本雏形
5. A6 开源复核台账初版

## Sprint-1 验收标准
- 后端测试新增并通过：
  - 玄学模块契约字段测试
  - 边界样本最小集测试
- 文档齐全：
  - 开源依赖复核台账
  - 变更日志（本次算法改动）

---

## 六、协作分工建议

- 你（产品/策略）：
  - 决定每模块解释风格与风险提示语气
  - 审核依赖库合规结论
- 我（Codex）：
  - 完成后端实现、测试、脚本、文档
  - 维护回归与发布门禁
- 顾问 Agent：
  - 提供算法方向评审与替代方案
  - 不直接作为“依赖已可用”结论来源（需本地复核）

---

## 七、任务状态模板（后续更新）

- TODO：未开始
- DOING：进行中
- BLOCKED：阻塞
- DONE：已完成（附提交号）

示例：
- A1 统一输出契约：DOING（branch: feat/astro-v2-contract）
- A2 八字重构：TODO
