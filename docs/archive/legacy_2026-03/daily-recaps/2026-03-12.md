# Daily Recap - 2026-03-12

## 1. 今日目标
- 修复并验证开发环境，确认 Docker / Java / Android / Gradle 链路可用。
- 启动阶段 A 开发，完成 Laravel 主线迁移收口。
- 打通 Android 到 Laravel 的联调主链路。
- 完成当日代码提交并推送到 GitHub 分支。

## 2. 关键问题与处理过程
- Docker / adb / Gradle 检查在沙箱内出现权限噪音，切换到沙箱外复核后确认主机环境可用。
- Laravel 本地依赖安装最初失败：
  - 原因 1：PHP 版本与 lock 不匹配（后升级到 PHP 8.5.3）。
  - 原因 2：Composer 访问 GitHub 失败（代理变量需同时配置 HTTP_PROXY + HTTPS_PROXY）。
- Android 首次联调失败：
  - Cleartext policy 拒绝 `10.0.2.2` 明文 HTTP。
  - 通过 `network_security_config` 精准放行开发地址解决。
- `chat:ws` 初版在 Windows 报 `pcntl_alarm`：
  - 将 `Timer::add` 挂载到 `onWorkerStart`，避免在错误生命周期调用。
- 登录/注册 500：
  - 日志定位为 DB 驱动问题，切换 `.env` 到 SQLite 本地方案并执行 `migrate:fresh --seed` 解决。

## 3. 阶段 A 完成内容
- 后端（Laravel）：
  - 聊天 HTTP 接口迁移：发送 / 拉取 / 已读。
  - 聊天模型与迁移：`chat_messages`。
  - Admin 最小审核流迁移：用户列表、禁用、审核队列、审核状态更新。
  - WebSocket 网关落地：`php artisan chat:ws --port=8081`（Workerman，DB 轮询推送）。
- Android：
  - 问卷接口对齐到 `/api/v1/questionnaire/*`。
  - 认证与消息模型对齐 Laravel 返回结构。
  - API 基址对齐 `10.0.2.2:8080`，WS 对齐 `10.0.2.2:8081`。
  - 新增网络安全配置，允许本地开发地址 cleartext。
- 文档与脚本：
  - 迁移计划与交接文档更新。
  - 新增启动脚本：`scripts/start_laravel_realtime_local.ps1`。
  - 新增就绪检查与推送脚本，并将敏感配置迁移到工程外目录。

## 4. 当日验证结果
- `composer install` 成功（代理修正后）。
- `artisan migrate:fresh --seed --force` 成功。
- `artisan test` 全通过（6 tests, 42 assertions）。
- Android `assembleDebug` 成功。
- 实机联调确认：
  - 注册、登录、进入答题、保存答案成功。
  - “进入匹配”返回 404（当前业务规则下无当周匹配/未发布 Drop，属预期）。

## 5. 当前状态结论
- 阶段 A 主链路已打通，系统从“环境不可用”进入“可持续联调开发”状态。
- 剩余工作主要是匹配链路的开发态可演示能力（非基础设施阻塞）。

## 6. 明日建议
- 增加开发态匹配数据生成与 Drop 发布命令/接口，消除匹配页固定 404。
- 补一轮 Android 与 Laravel 异常响应字段对齐（统一错误处理）。
- 完成 FastAPI 切流前的端到端回归清单，满足进入阶段 B 的门槛。
