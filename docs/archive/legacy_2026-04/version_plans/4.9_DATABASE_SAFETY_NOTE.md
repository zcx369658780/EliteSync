# 4.9 Database Safety Note

更新时间：2026-04-29

## 目的

确保 4.9 期间涉及数据库、缓存、回填、恢复或快照的动作都有可回滚路径。

## 当前原则

- 优先只读检查；
- 任何写入前先确认备份与回滚点；
- 不做 repo 级恢复；
- 不把前端缓存当作真值。

## 建议记录项

- 当前 schema / migration 版本
- 关键表快照时间
- 备份位置
- 恢复方式
- 验证命令

## 关注对象

- `users`
- `profiles`
- `dating_matches`
- `rtc_sessions`
- `rtc_session_events`
- `notifications`
- `media_assets`
- `media_processing_jobs`

## 已完成的正式演练

### 1. 云端只读迁移检查

- 服务器 `101.133.161.203` 上的 `php artisan migrate:status --no-interaction` 已通过；
- 当前迁移状态显示所有已存在迁移均为 `Ran`，包括：
  - `users`
  - `questionnaire_questions`
  - `questionnaire_answers`
  - `dating_matches`
  - `chat_messages`
  - `status_posts`
  - `conversations`
  - `conversation_members`
  - `media_assets`
  - `media_processing_jobs`
  - `message_attachments`
  - `notifications`
  - `rtc_sessions`
  - `rtc_session_events`

### 2. 备份演练

- 备份来源：`/opt/elitesync/services/backend-laravel/.env` 所指向的主库；
- 备份位置：
  - 云端：`/opt/backups/elitesync/mysql/20260429_174334/`
  - 本地：`D:\EliteSync\backups\aliyun_mysql\20260429_174334\`
- 备份产物：
  - `elitesync_20260429_174334.sql.gz`
  - `elitesync_20260429_174334.sql.gz.sha256`
  - `manifest.json`
- 本地 SHA256 与远端校验串一致，说明备份下载链路完整。

### 3. 临时库恢复演练

- 恢复目标库：`elitesync_restore_20260429`
- 恢复方式：将备份包恢复到独立数据库，不覆盖主库；
- 恢复结果：成功。

### 4. 关键只读比对

主库与临时库的关键计数一致：

- `information_schema.tables` 表数：`32` / `32`
- `users`：`2` / `2`
- `dating_matches`：`2` / `2`
- `conversations`：`1` / `1`
- `notifications`：`208` / `208`
- `rtc_sessions`：`107` / `107`

### 5. 演练后清理

- 临时恢复库 `elitesync_restore_20260429` 已在验证完成后删除；
- 云端仅保留正式备份包与校验文件，不保留额外测试库残留。

### 结论

当前 4.9 数据库安全与回滚路径已经不再只是骨架，而是已经补入了完整的只读 + 备份 + 临时库恢复 + 关键表比对演练证据。
这说明数据库 schema 与主要业务表可作为 4.9 release gate 的正式安全基线。

后续每次跨层恢复前，仍需继续补齐更细的表级恢复脚本与回滚演练记录；本文件继续作为数据库安全与回滚路径的正式证据基线。
