# 4.6A 验收摘要

更新时间：2026-04-23

## 当前结论

`4.6A` 当前更接近：

- `conditional pass`
- 中文口径：`连接层最小修复已落地，设备侧 fresh connect 复验仍待完成`

本轮已经确认并固定的内容：

- 云端 LiveKit 入口从旧的公网直连 `:7880` 改为公网根 host + nginx `/rtc/` 反代
- `LIVEKIT_URL` 已统一为 `http://101.133.161.203`
- `/rtc/validate` 从云端本机与外部网络都已可达
- Flutter / backend 两侧已加最小诊断日志
- 设备端已重装最新 APK，并能看到最新 `RTC_INVITE_PROVIDER_*` 诊断输出

当前仍缺的关键证据：

- 干净重启后的 `Room.connect()` 稳定成功证据
- 接听后进入 `TrackPublished` / `TrackSubscribed` / `AudioPlayback` 的证据
- 真机与模拟器双向都完成一轮“接听并进入通话中”的 fresh 复验

## 当前建议口径

如果顾问当前要判断版本状态，建议仍然写作：

> `4.6A` 已完成连接层修复与最小诊断补强，但还需要一轮干净重启后的设备侧接听复验，才能把 `Room.connect()` 成功与后续音频阶段一起收口。

## 已完成项

1. 云端 LiveKit 入口固定化
2. nginx `/rtc/` 反代固定化
3. `/rtc/validate` 可达性确认
4. Flutter 端 join-info / connect / room snapshot / audio snapshot 诊断日志
5. backend join-info 生成日志
6. backend RTC 接口请求日志

## 未完成项

1. 真机端在 fresh restart 后稳定进入 `Room.connect()`
2. 模拟器端在 fresh restart 后稳定进入 `Room.connect()`
3. 接听后进入 publish / subscribe / playback 的可观察证据
4. 最终 `pass with observations` 或更高结论的正式归档条件确认
