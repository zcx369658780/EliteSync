# 4.8 Smoke Execution Note

## Scope
- Emulator-only smoke run performed on 2026-04-28.
- Current adb visibility at the time of capture started as emulator only; phone was later reconnected and joined the same call flow.
- No source code changes in this note.

## Verified Pages
- Home / state stream baseline
- Messages list
- Chat page with visible voice call entry
- Call page / RTC voice skeleton
- Notification center
- Match page
- Me page
- Settings / beta operations / health section

## Observations
- Bottom navigation remains present on the modern UI baseline.
- Chat page exposes a visible `语音通话` entry.
- RTC call page opened successfully for `call_id: 107 / a06a16d4-9a7d-4cc2-99bc-264514bb0064`.
- The call is now connected on both sides:
  - phone side: `in_call / 接听方 / heartbeat / 通话进行中`
  - emulator side: `accepted / connected / heartbeat`
- Audio diagnostics progressed from `等待远端音轨…` to the user-confirmed state where audio frames were visibly appearing during the live call.
- Notification center still shows engineering slugs such as `rtc_call_ended`, `rtc_call_accepted`, `rtc_call_invite`.
- Me page shows the user profile and service-backed profile data.
- Settings page exposes account, privacy, performance, and beta ops sections.

## Blocking Notes
- Dual-device RTC verification is now materially progressed, but the call still needs a clean post-call capture and any remaining RTC/media observations should be written as blocker notes rather than guessed from screenshots.
- Version-center/health information is currently represented by the settings / beta operations section screenshots.

## Screenshot Evidence
See `4.8_UI_BASELINE_EVIDENCE_INDEX.md`.
