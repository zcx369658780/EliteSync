# 4.9 Smoke Matrix

更新时间：2026-04-29

## 目标

定义 4.9 Alpha smoke 的最小检查路径，确认主链可达、现代 UI 不回退、RTC 真实路径可复验。

## Smoke 覆盖

- 登录
- 首页
- 消息列表
- 聊天页
- 通知中心
- 匹配页
- 我的页
- 设置页
- 版本中心
- RTC 语音入口

## 当前状态

- smoke 范围已定义；
- UI / RTC / 通知 / 版本中心的检查点已列出；
- emulator 侧主页面、通知中心、RTC 通话页已得到现场证据；
- phone 侧主页面、版本中心、RTC 主链已得到现场证据；
- phone 侧通知中心独立页仍保留 observation，入口级证据已记录。

## 记录模板

| 页面 / 模块 | 手机 | 模拟器 | 备注 |
|---|---|---|---|
| 登录 | 通过 | 通过 | 两端均已登录；模拟器一度因系统网络/锁屏问题需要重试，但最终恢复 |
| 首页 | 通过 | 通过 | 现代首页 / 发现式 feed 与底部 Tab 可见 |
| 消息列表 | 通过 | 通过 | 搜索、去匹配、通知入口、当前会话卡片可见 |
| 聊天页 | 通过 | 通过 | 附件入口、输入框、历史消息可见 |
| 通知中心 | 入口级证据 / observation | 通过 | phone 侧首页卡片可见但独立页未稳定；emulator 侧真页面稳定 |
| 匹配页 | 通过 | 通过 | 现代匹配卡片结构可见 |
| 我的页 | 通过 | 通过 | 账号状态、基础资料、资料完整度可见 |
| 设置页 | 通过 | 通过 | 设置中心、Beta 运营准备、内容缓存、退出登录可见 |
| 版本中心 | 通过 | 通过 | 当前产品版本、模块版本、服务健康、更新历史可见 |
| RTC 语音 | 通过 | 通过 | 双端真实通话已成立，音频帧在现场联测中确认出现 |

## 结论

4.9 smoke 已完成大部分真实路径验证；phone 侧通知中心独立页仍保留 observation，但不影响主链 smoke 的通过判定。
