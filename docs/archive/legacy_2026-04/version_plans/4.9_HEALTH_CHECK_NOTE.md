# 4.9 Health Check Note

更新时间：2026-04-29

## 目的

定义 4.9 的最小健康检查集合，保证版本中心、API、RTC、通知、媒体链都能快速探活。

## 已验证项目

### 1. `/up`

- 结果：`200 OK`
- 说明：云端 HTTP 基础入口正常返回 `EliteSync` 应用页。

### 2. `/api/v1/app/version/check`

- 请求：`version_name=0.04.06&version_code=40600&platform=android`
- 结果：`200 OK`
- 结论：`has_update=false`
- 说明：当前客户端版本与发布基线一致。

### 3. `/downloads/elitesync-0.04.06.apk`

- 结果：`200 OK`
- `Content-Length=207625485`
- 说明：下载链路可达，APK 包体可直接获取。

### 4. `php artisan migrate:status --no-interaction`

- 结果：通过
- 说明：云端数据库 schema 已全部迁移完成，现有 migration 全部为 `Ran`。

### 5. 数据库正式演练

- 结果：通过
- 说明：已完成主库备份、独立临时库恢复与关键表计数比对，演练后临时库已清理。

## 待补项目

- `GET /api/v1/notifications/unread-count`
- `GET /api/v1/rtc/calls/{callId}`（真实 smoke 现场可继续补）
- `GET /api/v1/rtc/calls/{callId}/livekit`（真实 smoke 现场可继续补）
- 媒体内容读取（现场失败路径可继续补）

## 输出模板

- 检查时间：
- 检查范围：
- 通过项：
- 失败项：
- 观察项：

## 结论

当前最小健康检查已验证 HTTP、版本检查、下载链路、迁移状态与数据库正式演练可用；通知 / RTC / 媒体的最小健康检查仍可在 smoke 阶段继续补齐。
