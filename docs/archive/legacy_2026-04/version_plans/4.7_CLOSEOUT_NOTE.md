# 4.7 收尾说明

更新时间：2026-04-26

## 为什么可以收尾

4.7 的目标是稳定化与门禁化，而不是继续扩功能。

当前已经完成：

- UI protected surfaces 落地
- 回滚与恢复政策落地
- UI baseline evidence 补齐
- UI regression checklist 固化
- 事故修复说明归档

因此 4.7 已进入“可收尾、可交接、可由顾问复核”的状态。

## 收尾口径

建议把 4.7 视为：

> `pass with observations`

## 当前观察项

- 独立 live-call runtime state 可以作为额外补充证据
- 后续任何跨层恢复必须继续遵守 protected surfaces

## 收尾后默认规则

1. 不再默认允许 repo 级恢复
2. 恢复前必须 checkpoint
3. 跨层 blocker 必须先写 blocker report
4. UI 保护面默认不可碰
5. smoke / regression 必须复查 UI baseline
