# Soul 设置模块 Blocker 报告

## Session

- Date: 2026-05-01
- Device: emulator-5554
- Package: `cn.soulapp.android`
- Module: 设置 / 主页背景设置面

## Blocker 概述

在 Soul 的设置入口中，当前已稳定进入的是个人页右上角 `设置` 打开的 `主页背景` 底抽屉。继续进入低风险的 `图片背景 / 去设置` 时，应用弹出系统权限请求：

- `Soul想访问你的相机和媒体文件`
- 英文系统文案：`Allow Soul to take pictures and record video?`

由于本次设置拆解目标要求**不进入高风险深区**，且相机/媒体权限会进一步引出背景素材选择流程，因此我未授予权限，也未继续深入背景素材选择页。

## 阻塞位置

- 页面入口：个人页右上角 `设置` 图标
- 当前已达页面：`主页背景`
- 触发 blocker 的入口：`图片背景 / 去设置`

## 影响范围

- 不影响当前已记录的低风险设置面内容：
  - `主页背景`
  - `我的空间装扮`
  - `立即开通`
  - `图片背景`
  - `互动背景`
- 仅阻止继续向下探索背景素材选择与可能的图片 / 相册 / 相机相关深层页

## 已确认的安全边界

- 未进入支付
- 未进入会员购买确认
- 未进入账号删除
- 未进入手机号绑定
- 未进入账号安全深区
- 未主动授权相机 / 媒体文件权限

## 建议的后续处理

如果后续要继续把 Soul 的设置面拆完整，可以有两种方式：

1. 保持低风险原则，不授予权限，仅把当前设置抽屉当作可观察样本；
2. 若用户明确允许，可临时授予 `While using the app` 级别权限，仅用于观察背景设置的浅层结构，不进入会员 / 支付 / 删除等高风险区域。

当前这次拆解不需要继续盲点推进，Blocker 已足够说明问题边界。
