# EliteSync 6.0-A1 Readonly Smoke Batch 1 Production Readonly Report

执行时间：2026-05-14 13:37:36 +08:00  
当前阶段：A1 readonly smoke batch 1 最小重跑  
授权类型：production-readonly-explicit  
BASE_URL：`http://101.133.161.203`  
报告定位：第一批最小只读 smoke 执行报告。本轮只执行公开只读 health、version check 与 release chain 本地只读文件核对。

## 0. 执行边界

- 本轮只允许执行：
  - `GET /api/v1/app/health`
  - `GET /api/v1/app/version/check?platform=android&version_name=0.00.01&version_code=1&channel=stable`
  - release chain local readonly file check
- 本轮未执行任何 POST。
- 本轮未请求或使用 token。
- 本轮未使用真实用户账号。
- 本轮未执行 profile / astro / messages / media / questionnaire / RTC / notifications smoke。
- 本轮未写 DB，未执行 migration，未修改 production 配置。
- 本轮未修改 `apps/`、`services/`、`database/`、`scripts/` 下任何 runtime 或 release 文件。
- 本轮未运行 release script，未生成 APK，未进入 v2 skeleton runtime。

## 1. Git 与环境状态

| 项 | 结果 |
| --- | --- |
| 当前分支 | `feature/5.0-alpha-readiness-20260501` |
| 当前 HEAD | `10eb1464` |
| git status 执行前 | clean |
| git status 执行后 | 仅新增本报告文件 |
| BASE_URL | `http://101.133.161.203` |
| 授权类型 | `production-readonly-explicit` |

## 2. Smoke 结果总表

| Smoke ID | 名称 | 结果 | HTTP status | 是否写 DB | 响应 / 文件摘要 |
| --- | --- | --- | --- | --- | --- |
| V1-P0-01 | app health | pass | 200 | no | `status=ok`，`app_version=0.05.10`，`app_version_code=51000`，database/config check 均为 ok |
| V1-P0-02 | app version check | pass | 200 | no | latest `0.05.10 / 51000`，min supported `0.01.01`，`has_update=true`，`force_update=true`，download 指向 `elitesync-0.05.10.apk` |
| V1-P0-18 | release chain local readonly | pass | n/a | no | 本地 release chain 文件无 diff，版本口径对齐 `0.05.10 / 51000` |

## 3. V1-P0-01 Health 响应字段摘要

只输出字段摘要，不包含 secret、内部 host、DB host、Redis host 或 `.env` 信息。

| 字段 | 摘要 |
| --- | --- |
| `status` | `ok` |
| `checked_at` | `2026-05-14T05:37:40+00:00` |
| `environment` | `local` |
| `app_version` | `0.05.10` |
| `app_version_code` | `51000` |
| `checks.database.ok` | `true` |
| `checks.database.driver` | `mysql` |
| `checks.config.ok` | `true` |
| `checks.config.message` | `app_update loaded` |

判定：`pass`。

Observation：production-readonly 请求下 health 返回的 `environment` 字段为 `local`。该项与既有 Gate/ops-hardening observation 一致，不影响本轮只读 smoke 结果，但应继续作为运维环境口径问题跟踪。

## 4. V1-P0-02 Version Check 响应字段摘要

只输出字段摘要，不包含 token、secret 或用户信息。

| 字段 | 摘要 |
| --- | --- |
| `platform` | `android` |
| `channel` | `stable` |
| `client_version_name` | `0.00.01` |
| `client_version_code` | `1` |
| `latest_version_name` | `0.05.10` |
| `latest_version_code` | `51000` |
| `min_supported_version_name` | `0.01.01` |
| `has_update` | `true` |
| `force_update` | `true` |
| `download_url` | `http://101.133.161.203/downloads/elitesync-0.05.10.apk` |
| `sha256` | present |
| `changelog` | present，内容指向 5.6-5.10 玄学能力二次产品化阶段收口与 `0.05.10 / 51000` 发布元数据 |

判定：`pass`。

版本目标口径核对：API 返回 `0.05.10 / 51000`，与本轮目标一致。

## 5. V1-P0-18 Release Chain Local Readonly 摘要

本轮只读检查以下文件，未修改、未 stage、未运行 release script：

- `apps/android/app/build.gradle.kts`
- `apps/android/app/src/main/assets/changelog_v0.txt`
- `apps/flutter_elitesync_module/assets/config/about_update_0_xx.json`
- `services/backend-laravel/config/app_update.php`
- `scripts/release_android_update_aliyun.ps1`

只读结果：

| 检查项 | 摘要 |
| --- | --- |
| release chain diff | empty |
| Android `versionCode` | `51000` |
| Android `versionName` | `0.05.10` |
| backend `app_update.php` | default latest `0.05.10 / 51000`，download URL 指向 `elitesync-0.05.10.apk` |
| Android changelog asset | 包含 `0.05.10` |
| Flutter about update history | 包含 `0.05.10` |
| release script | 未运行 |

判定：`pass`。

## 6. 版本一致性与保护面

| 项 | 结果 |
| --- | --- |
| 是否发现 release chain 版本不一致 | no |
| API 版本口径 | `0.05.10 / 51000` |
| Android 本地版本口径 | `0.05.10 / 51000` |
| backend release config 口径 | `0.05.10 / 51000` |
| changelog / about history | 包含 `0.05.10` |
| 是否请求 token | no |
| 是否输出 token | no |
| 是否写 DB | no |
| 是否修改文件 | no，除新增本报告外 |
| 是否修改 release chain | no |
| 是否进入 v2 skeleton | no |

## 7. 后续建议

- 是否允许进入更多 smoke：`conditional`。本轮最小只读 smoke 通过，但只覆盖公开 health、version check 和 release chain 本地只读核对；任何 auth smoke、profile / astro / messages / media / questionnaire / RTC / notifications smoke 仍需单独授权、smoke 账号、token 处理和脱敏规则确认。
- 是否允许进入 write smoke：`no`。
- 是否允许进入 v2 skeleton：`no`。仍需后续完整 v1 P0 smoke 范围、Gate P1 carry-over 与 GPT 顾问确认。
- 建议下一步：提交本阶段报告，之后再由用户/GPT 顾问决定是否授权下一批只读 smoke。
