# EliteSync 5.7 UI Baseline Evidence Index

更新时间：2026-05-11

## 1. Evidence Scope

5.7 UI 证据目标是证明 Match 关系解释层已经进入用户行动点，并保持 display-only。

核心文本：

- `为什么值得聊`
- `共同点`
- `表达节奏`
- `慢约会适配度`
- `建议这样开始`
- `先避免这样表达`
- `轻追问建议 · 敬请期待`
- `不会写入资料`
- `不会改变匹配算法`
- `不会自动发送消息`

## 2. Automated UI Evidence

Widget evidence：

- `apps/flutter_elitesync_module/test/features/match/presentation/widgets/relationship_explanation_card_test.dart`

Page evidence：

- `apps/flutter_elitesync_module/test/features/match/presentation/pages/match_result_page_test.dart`
- `apps/flutter_elitesync_module/test/features/match/presentation/pages/match_detail_page_test.dart`

通过命令：

```powershell
cd apps/flutter_elitesync_module
flutter test test/features/match/presentation/widgets/relationship_explanation_card_test.dart test/features/match/presentation/pages/match_result_page_test.dart test/features/match/presentation/pages/match_detail_page_test.dart
```

结果：

```text
All tests passed!
```

覆盖：

- 解释卡标题、三维度、建议 / 避免、轻追问 disabled 占位、display-only 免责声明。
- Match result 页面原 `进入首聊` / `查看完整解释` CTA 仍可见。
- Match detail 页面原 `解释到聊天` 区块仍可见。

## 3. Runtime Evidence Attempt

已安装并启动 Flutter debug host：

```powershell
flutter run -d emulator-5554 -t lib/main_dev.dart --dart-define=ELITESYNC_INITIAL_ROUTE=/match/result --no-resident
```

设备：

```text
emulator-5554
```

运行态证据文件：

- `docs/version_plans/assets/5.7/match/relationship_explanation_runtime_blocked.png`
- `docs/version_plans/assets/5.7/match/relationship_explanation_runtime_blocked.xml`

结论：

- App 已在 emulator 启动。
- Match tab 可达。
- 当前真实账号处于 Drop countdown / detail loading 状态，未能采到解释卡实屏。
- 本轮不伪造 runtime evidence，不把 countdown / loading 截图写成解释卡已可见。

## 4. Evidence Interpretation

本轮 UI 证据口径：

- 自动化 widget/page test 证明 5.7 解释层可渲染并包含验收文本。
- runtime screenshot/XML 证明本机 emulator 可运行更新后的 host，但真实数据状态阻挡了解释卡实屏采集。
- 真实 active match / 已揭晓数据可用后，应补采解释卡实屏作为 observation 收口。

## 5. Protected Surface Evidence

命令：

```powershell
git status --short -- services database routes config scripts/release_android_update_aliyun.ps1 apps/android/app/build.gradle.kts
```

结果：空。

说明：5.7 UI 证据收口没有触碰 backend / DB / API / release chain。
