# EliteSync 5.1F Voice Rhythm Evidence Index

更新时间：2026-05-02

## 证据状态

已补充当前 Android debug 包的模拟器截图证据。

## 已采集截图

目录：

`docs/version_plans/assets/5.1/voice/`

已采集：

- `../chat/chat_voice_rhythm_card.png`：聊天页关系摘要内显示 `语音节奏` 区块，并保留 `查看语音前提示` 入口。
- `chat_voice_before_call_dialog.png`：发起语音前弹窗显示 `语音前先确认节奏 / 继续文字 / 现在语音`。
- `rtc_result_missed_followup.png`：RTC 结果页显示 `未接通后的回聊建议 / 回到文字 / 低压接续 / 稍后再聊`。

未采集：

- `rtc_result_success_followup.png`：通话结束后显示 `通话后的继续建议`。

采集观察：

- 聊天页进入已有消息会话后会自动滚动到消息底部；截图 `../chat/chat_voice_rhythm_card.png` 通过手动回到顶部后采集，内容与 UI 层级中的 `语音节奏` 区块一致。

## 机器证据

已执行验证：

- `flutter test test/features/chat/presentation/pages/chat_room_page_test.dart`
- `flutter test test/features/rtc/presentation/pages/rtc_call_result_page_test.dart`
- `flutter test test/features/rtc/presentation/pages/rtc_call_page_test.dart test/features/rtc/presentation/pages/rtc_incoming_call_page_test.dart test/app/router/app_shell_rtc_invite_test.dart`
- `flutter analyze`

结果：

- ChatRoom widget 测试通过，覆盖 `语音节奏` 与 `语音前先确认节奏` 弹窗。
- RTC result widget 测试通过，覆盖 `未接通后的回聊建议` 与低压接续文案。
- RTC call / incoming call / router invite 回归测试通过，共 7 条。
- `flutter analyze` 未发现本轮新增 error / warning；仓库剩余 14 条 analyzer info，为既有非阻断项。

执行观察：

- 并行运行多个 `flutter test` 曾触发 `build/unit_test_assets/NativeAssetsManifest.json` 临时文件竞争；改为单条命令串行覆盖后通过。

## 验收注意

- 截图必须来自当前包。
- 文件名、截图内容、页面实际内容必须一致。
- 不得把本轮写成 LiveKit 或 RTC 状态机改造。
- 不得声称自动生成或自动发送回聊消息。
