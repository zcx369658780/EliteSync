# EliteSync 5.1D Content To Chat Evidence Index

更新时间：2026-05-01

## 证据状态

当前为实现记录索引，正式截图尚未采集。

## 待采集截图

建议目录：

`docs/version_plans/assets/5.1/content/`

待采集：

- `status_card_low_pressure_cta.png`：状态卡展示 `继续了解 / 看看资料 / 稍后再聊 / 低压私聊`。
- `status_author_returnflow.png`：作者页展示 `低压回流` 卡。
- `status_later_prompt.png`：点击 `稍后再聊` 后显示本地提示。
- `status_low_pressure_chat_guard.png`：点击 `低压私聊` 时先提示匹配关系限制，再进入作者页。

## 机器证据

已执行验证：

- `flutter test test/features/status/presentation/widgets/status_post_card_test.dart`
- `flutter test test/features/status/domain/entities/status_post_entity_test.dart`
- `flutter test test/features/status/domain/entities/status_author_entity_test.dart`

结果：通过。

静态检查：

- `flutter analyze`

结果：未发现本轮新增 error / warning；仓库剩余 15 条 analyzer info，均在非本轮文件中，主要为既有 `avoid_print`、null-aware style 和 RTC test field 提示。

## 验收注意

- 截图必须来自当前包。
- 文件名、截图内容、页面实际内容必须一致。
- 不得把状态流描述为重社区。
- 不得把本地 `稍后再聊` 提示写成服务端收藏或服务端队列。
