# EliteSync 2.9 Rollback Rehearsal

更新时间：2026-04-08

## 1. 演练目标

验证 2.9 在灰度/发布异常时，是否具备清晰的回滚点、可执行的回滚步骤，以及回滚后最小验收路径。

## 2. 当前可用回滚点

- `scripts/release_android_update_aliyun.ps1`
- `scripts/deploy_aliyun_backend.ps1`
- `scripts/smoke_backend_alpha.ps1`
- `scripts/release_gate_alpha.ps1`
- `scripts/regression_alpha_baseline.ps1`
- `services/backend-laravel/config/app_update.php`
- `apps/android/app/build.gradle.kts`
- `docs/ROLLBACK_PLAN.md`
- `docs/version_plans/2.9_BETA_RELEASE_CHECKLIST.md`
- `docs/version_plans/2.9_BETA_RELEASE_REHEARSAL.md`

## 3. 演练步骤（Dry Run）

1. 确认当前版本与下载地址为 `0.02.08 / 208`
2. 确认 `health` 与 `version/check` 均返回正常
3. 预设回滚目标为上一个稳定发布版本
4. 恢复 `app_update.php` 至上一个稳定版本配置
5. 重新部署 backend
6. 重新跑 `health` / `version/check` / smoke
7. 确认主链路可见性恢复

## 4. 已确认结果

- `health`：PASS
- `version/check`：PASS
- 最小 release gate 预演：PASS
- Host APK 冷启动：PASS

## 5. 回滚后最小验收

- 首页可见
- 资料页可见
- 匹配页可见
- 消息页可见
- about 页可见
- 版本检查正常

## 6. 备注

当前这次为 **dry run**，未在生产做真实回滚，以避免影响现有可用链路。
