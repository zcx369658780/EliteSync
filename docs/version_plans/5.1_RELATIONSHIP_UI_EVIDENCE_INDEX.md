# EliteSync 5.1C Relationship UI Evidence Index

更新时间：2026-05-01

## 证据状态

当前为实现记录索引，正式截图尚未采集。

## 待采集截图

建议目录：

`docs/version_plans/assets/5.1/chat/`

待采集：

- `match_detail_to_chat_action.png`：完整解释页显示 `解释到聊天` 行动卡。
- `match_detail_draft_success.png`：点击后提示已写入聊天草稿。
- `chat_room_from_match_detail_draft.png`：跳转到 ChatRoom 后输入框存在草稿。

## 机器证据

已执行验证：

- `flutter analyze`
- `flutter test test/features/chat/presentation/pages/chat_room_page_test.dart`
- `flutter test test/features/chat/presentation/widgets/message_input_bar_test.dart`
- `flutter test test/features/chat/data/dto/message_dto_test.dart`
- `flutter test test/features/chat/data/datasource/chat_socket_data_source_test.dart`

结果：

- Chat 定向测试通过。
- `flutter analyze` 未发现本轮新增 error / warning；仓库现有 19 条 analyzer info 仍存在。

未完成：

- 后续如补 Match Detail widget test，应增加 `match_detail_page_test.dart` 覆盖行动卡。

## 验收注意

- 必须确认写入的是草稿，不是自动发送消息。
- 必须确认跳转参数是 peerId。
- 截图不得混用 5.0 旧证据。
