# EliteSync 5.1E Notification Evidence Index

更新时间：2026-05-02

## 证据状态

已补充当前 Android debug 包的模拟器截图证据。

## 已采集截图

目录：

`docs/version_plans/assets/5.1/notification/`

已采集：

- `notification_voice_returnflow.png`：通知中心显示语音类回流卡片，包含 `语音邀请 / 处理语音 / 稍后处理 / 标记已读`，并显示 `将回到语音通话相关页面。`

未采集：

- `notification_message_returnflow.png`：消息通知显示 `消息回流 / 继续回复`。
- `notification_status_returnflow.png`：动态互动通知显示 `动态互动 / 去看看`。
- `notification_match_returnflow.png`：匹配推进通知显示 `匹配推进 / 查看匹配`。
- `notification_later_and_read.png`：独立展示 `稍后处理 / 标记已读` 动作的更窄范围截图。

已删除旧截图 / 无效证据：

- 采集时间早于当前 APK 重装的旧通知中心截图已删除，不再作为 5.1E 正式截图证据。

## 机器证据

已执行验证：

- `flutter test test/features/notification/presentation/pages/notification_center_page_test.dart`
- `flutter analyze`

结果：

- 通知中心 widget 测试通过。
- 覆盖 `消息回流 / 继续回复 / 稍后处理 / 标记已读 / 将回到对应聊天页。` 渲染。
- 覆盖无 route 通知的 `仅标记已读` 主动作会调用 `markRead`。
- 覆盖已知 kind 但无 route 时，主动作仍显示 `仅标记已读`，不会显示误导型 `继续回复`。
- 覆盖未知 route 的 `打开` 主动作不会误调用 `markRead`。
- 覆盖未知 route 的卡片点击不会误调用 `markRead`。
- `flutter analyze` 未发现本轮新增 error / warning；仓库剩余 15 条 analyzer info，均在非本轮文件中。

## 验收注意

- 截图必须来自当前包。
- 文件名、截图内容、页面实际内容必须一致。
- 如果 route 缺失，必须保留真实失败提示，不得伪成功。
- 不得把站内通知描述为系统厂商推送。
