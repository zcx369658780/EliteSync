# EliteSync 6.0-A1 Option B Readonly Route / Runtime Diagnostic Report

## 0. One-line conclusion

The most likely blocker category is deployed-code staleness: the readonly diagnostic was executed in the authorized server SSH context, port `127.0.0.1:8088` is locally listened to, the Option B symlink target still matches, but the deployed Laravel tree does not contain the expected R1 v2 controller/test source files and runtime route inventory has no `api/v2` routes.

## 1. Baseline

| Item | Result |
|---|---|
| issue number | `#52` |
| branch | `feature/5.0-alpha-readiness-20260501` |
| starting HEAD | `075b2941655a04d706404f8633a64a570675af38` |
| worktree state before SSH | clean |
| known 404 blocker status | issue #49 server-side localhost verification returned HTTP 404 JSON responses for all three expected v2 endpoints |
| public staging request | no |
| production request | no |

## 2. Execution context proof

The diagnostic commands were executed after SSH on the authorized EliteSync Alibaba Cloud server, not on the user's Windows PC.

Redacted server-localhost context summary:

| Item | Result |
|---|---|
| SSH executed | yes |
| hostname | captured from remote server |
| user | `root` |
| working directory | `/root` |
| timestamp UTC | `2026-05-18T11:41:46Z` |
| context note | commands executed after SSH on authorized server |

This evidence distinguishes server-localhost from PC-localhost. The user's PC-side Laravel backend absence may explain PC-localhost failures, but this diagnostic points to the server deployed runtime surface.

## 3. Listener / Nginx layer summary

Readonly listener / symlink checks showed:

| Item | Result |
|---|---|
| `127.0.0.1:8088` listener | present |
| listener process details | redacted |
| Option B sites-enabled symlink | exists |
| resolved symlink target | `/etc/nginx/sites-available/elitesync_staging_ip.conf` |
| symlink target matched expected | yes |

No full Nginx config body was printed.

## 4. Deployed Laravel source presence summary

The likely deployed backend directory exists:

```text
/opt/elitesync/services/backend-laravel
```

Readonly source presence checks:

| path | present on server |
|---|---|
| `public/index.php` | yes |
| `routes/api.php` | yes |
| `app/Http/Controllers/Api/V2/AppHealthController.php` | no |
| `app/Http/Controllers/Api/V2/AppReadinessController.php` | no |
| `app/Http/Controllers/Api/V2/Contracts/LocationContractController.php` | no |
| `tests/Feature/Api/V2/AppHealthTest.php` | no |
| `tests/Feature/Api/V2/AppReadinessTest.php` | no |
| `tests/Feature/Api/V2/LocationContractTest.php` | no |

Readonly route source token checks in server `routes/api.php` found no expected R1 v2 route/controller tokens:

| token | present in deployed `routes/api.php` |
|---|---|
| `api/v2/app/health` | no |
| `api/v2/app/readiness` | no |
| `api/v2/contracts/location` | no |
| `v2/app/health` | no |
| `v2/app/readiness` | no |
| `v2/contracts/location` | no |
| `AppHealthController` | no |
| `AppReadinessController` | no |
| `LocationContractController` | no |

No source file body was printed.

## 5. Runtime route surface summary

Readonly route inventory command:

```text
php artisan route:list --path=api/v2
```

Result summary:

```text
Your application doesn't have any routes matching the given criteria.
```

The three expected v2 routes did not appear in runtime route inventory:

| expected route | present in runtime route inventory |
|---|---|
| `GET api/v2/app/health` | no |
| `GET api/v2/app/readiness` | no |
| `GET api/v2/contracts/location` | no |

The initially attempted `--columns` option was not supported by the deployed Artisan command and was not used for the final route inventory summary.

## 6. Cache/runtime state summary

Readonly cache file existence / timestamp summary:

| cache file | present | observation |
|---|---|---|
| `bootstrap/cache/routes-v7.php` | no | no route cache file found under this name |
| `bootstrap/cache/routes.php` | no | no route cache file found under this name |
| `bootstrap/cache/config.php` | yes | config cache file exists; not read or modified |
| `bootstrap/cache/packages.php` | yes | package cache file exists; not read or modified |
| `bootstrap/cache/services.php` | yes | service cache file exists; not read or modified |

No cache clear, config clear, route clear, optimize clear, route cache, or config cache command was executed.

## 7. Likely root-cause category

```text
deployed-code staleness
```

Rationale:

- The diagnostic was executed from server-localhost context, reducing likelihood of PC-localhost mismatch as the primary cause.
- Port `127.0.0.1:8088` is locally listened to and the Option B symlink target still matches.
- The deployed backend directory exists and appears to be a Laravel tree.
- The deployed tree does not contain expected R1 v2 controller/test source files.
- Deployed `routes/api.php` does not contain expected R1 v2 route/controller tokens.
- Runtime route inventory reports no matching `api/v2` routes.

This evidence is more consistent with the server deployed code not containing the repo R1 v2 runtime slice than with a pure path-prefix mismatch. Nginx-to-Laravel routing mismatch is still possible, but it is not the strongest current explanation because the deployed Laravel route surface itself lacks the expected routes.

## 8. What was not done

- no public staging request
- no production request
- no endpoint verification rerun
- no authenticated endpoint request
- no credential read
- no `.env` / `.env.*` content read or printed
- no full Nginx config body printed
- no config modification
- no cache clear / config clear / route clear / optimize clear
- no DB / migration / seed
- no Nginx reload / restart
- no runtime code change
- no tests / DB / env / server config / Flutter / release chain change

## 9. Recommended next gate

Recommended next gate:

```text
prepare deployment/sync authorization package
```

This diagnostic does not authorize deployment, sync, endpoint verification rerun, public staging request, production request, cache refresh, Nginx correction, Candidate C implementation, A1 final acceptance, production readiness, or A2.
