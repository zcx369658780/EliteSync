# EliteSync 5.5 Feedback Evidence Index

Updated: 2026-05-08

Purpose: index screenshots, XML, logs, and read-only checks used to support 5.5 feedback decisions.

## 1. Evidence Rules

- Evidence must come from the current `0.05.04 / 50400` host package unless explicitly marked as historical reference.
- Screenshots and XML must be paired when possible.
- Logs or API checks must be read-only unless the user explicitly approves a write operation.
- Do not claim Cloud DB, backup, restore, migration, queue logs, or RTC success passed without current approved evidence.
- Do not use old 5.4 `0.04.09 / 40900` screenshots as 5.5 current-package evidence.

## 2. Evidence Index

| ID | Feedback ID | Surface | Screenshot | XML / Log / Command | Source Build | Result | Meaning |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 5.5-EV-001 | 5.5-FB-001 | Status Plaza | User supplied Image #1 in current session | TBD | `0.05.04 / 50400` presumed from current emulator baseline | Accepted as user feedback evidence | Shows status publish failure snackbar with a Dart response-shape cast error. |
| 5.5-EV-002 | 5.5-FB-002 | Match Detail | User supplied Image #2 in current session | TBD | `0.05.04 / 50400` presumed from current emulator baseline | Accepted as user feedback evidence | Shows match detail page stuck in an empty / skeleton loading state. |
| 5.5-EV-003 | 5.5-FB-003 | Me / Profile | User supplied Image #3 in current session | TBD | `0.05.04 / 50400` presumed from current emulator baseline | Accepted as user feedback evidence | Shows floating status publish action overlapping lower profile content. |
| 5.5-EV-004 | 5.5-FB-004 | Astro Overview | User supplied Image #4 in current session | TBD | `0.05.04 / 50400` presumed from current emulator baseline | Accepted as user feedback evidence | Shows Ziwei overview preview clipping the bottom row of the 4x4 palace grid. |
| 5.5-EV-005 | 5.5-FB-001 | Status Plaza | Claude Appium read-only walkthrough | Appium page source / screenshots from current session | `0.05.04 / 50400` | UI precheck passed | Confirms Status Plaza, publish form, visibility chips, cover controls, and status feed are visible; final publish remains user/manual verification because it writes data. |
| 5.5-EV-006 | 5.5-FB-002 | Match Detail | Claude Appium read-only walkthrough | Appium page source from current session | `0.05.04 / 50400` | Data-dependent uncertain | Confirms Match Detail no longer appears as only infinite skeleton after refresh; it shows `详情加载失败` / `no match statusCode=404`, so success state requires active match data retest. |
| 5.5-EV-007 | 5.5-FB-003 | Me / Profile | Claude Appium screenshots: `screenshot_1778160546008.png`, `screenshot_1778160728249.png`, `screenshot_1778160747766.png` | Appium page source from current session | `0.05.04 / 50400` | Confirmed fail before fix | Confirms the `发布状态` floating action persists across top/middle/bottom profile scroll positions and competes with content and bottom navigation. |
| 5.5-EV-008 | 5.5-FB-004 | Astro Overview | Claude Appium structural check | Appium page source from current session | `0.05.04 / 50400` | Structural pass after fix | Confirms Ziwei overview palace labels are reported displayed, including bottom row; Claude notes pixel-level visual confirmation remains useful. |
| 5.5-EV-009 | 5.5-FB-005 | Status Author Profile | Claude Appium walkthrough | Appium page source from current session | `0.05.04 / 50400` | Confirmed fail before fix | Confirms Status Plaza -> author profile can show `作者资料加载失败` with Map/List cast error. |
| 5.5-EV-010 | 5.5-FB-003 | Me / Profile + Home | Claude Appium regression smoke | Screenshots from current session | `0.05.04 / 50400` | Passed | Confirms Profile no longer shows floating `发布状态` after scroll, while Home still exposes `发布状态` entries. |
| 5.5-EV-011 | 5.5-FB-005 | Status Author Profile | Claude Appium regression smoke | Appium page source from current session | `0.05.04 / 50400` | Passed | Confirms Status Plaza -> author profile renders user info, low-pressure return section, and recent statuses without Map/List cast error. |
| 5.5-EV-012 | Fixed surfaces | Home / Profile / Status Author / Astro / Match | Claude Appium fixed-surface smoke | Appium page source from current session | `0.05.04 / 50400` | Passed | Confirms fixed-surface smoke: Home publish entry, Profile no floating publish action, Status Author loads, Astro Ziwei structure complete, Match no-match state not infinite skeleton. |
| 5.5-EV-013 | Exploratory smoke | Home / Discover / Match / Messages / Notification / Profile / Status / Astro / Version / RTC | Claude Appium exploratory smoke | Appium page source from current session | `0.05.04 / 50400` | 9 pass, 0 fail, 3 uncertain | Claude checked 12 surfaces for visible crashes, infinite skeletons, cast errors, blocked empty states, severe overlap, and missing content. No fail was found. Notification Center standalone route, Version Center, and RTC Permission were marked uncertain because current read-only navigation could not reach them through primary paths in that run; visible inline cards/areas rendered normally. |
| 5.5-EV-014 | Direct-route smoke | Notification Center / Version Center / RTC Permission | Claude Appium direct-route attempt | Appium page source and route-code review from current session | Current app session was unauthenticated | Auth-gated uncertain | Claude found the three routes are behind GoRouter auth guard and current Appium session was on login/register pages. No external deep-link for these routes was found, so the pages need a logged-in session or user-approved smoke-account login before direct-route smoke can close. |
| 5.5-EV-015 | Logged-in guarded smoke | Notification Center / Version Center / RTC Permission / RTC call page | Claude Appium logged-in smoke with user-approved smoke account | Appium page source from current session | `0.05.04 / 50400` | Passed | User authorized smoke login with `17094346566`; Claude confirmed logged-in Profile, Messages / notification surface, Version Center, and RTC voice-call path are reachable with no crash, red screen, cast error, infinite skeleton, or severe overlap. RTC permission page was skipped because microphone permission was already granted; this is route reachability evidence, not LiveKit media success evidence. |
| 5.5-EV-016 | RTC dual-device prep | Emulator + connected phone | ADB package/version check and install verification | `adb devices -l`; `adb shell dumpsys package com.elitesync`; `scripts/fast_deploy_flutter_android_debug.ps1 -DeviceId TG9L8HOBKFMJZTZX` | Emulator `0.05.04 / 50400`; phone updated to `0.05.04 / 50400` | Device baseline ready | ADB sees phone `TG9L8HOBKFMJZTZX` and emulator `emulator-5554`. The phone was updated from `0.04.04 / 40400` to `0.05.04 / 50400` through USB debug install, and account 2 cache confirms `13772423130`. This removes the version-baseline blocker for current-package dual-device RTC testing. |
| 5.5-EV-017 | RTC dual-device attempt | Emulator caller + phone receiver | ADB screenshots / UI XML from both devices | `.claude/logs/emulator_call_new.png`; `.claude/logs/emulator_call_final.png`; `.claude/logs/phone_messages_during_call.png`; ADB UI XML dumps | Both devices `0.05.04 / 50400` | Risky pass / receiver not closed | Emulator account `17094346566` reached RTC call page and created `call_id: 110 / f232d48f-cd13-4e44-bde6-f623440101dc`, showing `calling` / `created` and later `missed` / `{reason: timeout}`. Phone account `13772423130` was on the Messages surface with the related conversation visible, but no incoming-call UI or accept path was observed before timeout. This proves caller-side RTC state-machine reachability, not LiveKit media success or receiver-side call acceptance. |
| 5.5-EV-018 | 5.5-FB-006 | RTC receiver-side watcher | Phone UI XML from current session | ADB logcat; `/api/v1/rtc/calls` create/query/end checks; `call_id: 114` | Phone and emulator installed current debug build from `0.05.04 / 50400` source line | Receiver path verified / media still partial | Before fix, logcat showed `RTC_INVITE_PROVIDER_ERROR NoSuchMethodError: AsyncData<SessionState> has no instance method maybeWhen`, while API showed the receiver account could see active incoming calls. After fix, API-assisted call `114` made the phone auto-enter `语音通话`, status `in_call`, with heartbeat and local-audio-frame logs; test call was ended through API. This verifies receiver incoming-call scan and accept/connect path, not full two-device LiveKit media success because the caller side was API-created and Aliyun backend timeout remains 10s until deployment. |
| 5.5-EV-019 | 5.5-FB-006 | RTC app-to-app LiveKit call | `docs/version_plans/assets/5.5/screenshots/rtc_call115_emulator.png`; `docs/version_plans/assets/5.5/screenshots/rtc_call115_phone.png` | ADB logcat; `/api/v1/rtc/calls/115`; XML: `docs/version_plans/assets/5.5/xml/rtc_call115_emulator.xml`, `docs/version_plans/assets/5.5/xml/rtc_call115_phone.xml`; user audible confirmation | Phone and emulator installed current debug build from `0.05.04 / 50400` source line | Passed / app-to-app media verified | Emulator account `17094346566` opened chat with phone account `13772423130`, tapped `语音通话`, confirmed `现在语音`, and created app-to-app `call_id: 115`. Server state reached `created -> accepted -> connected -> heartbeat -> ended`; both sides emitted LiveKit local audio frames and remote audio frame / stats evidence. The user reported hearing phone-side audio capture during the call. Cleanup force-stopped both apps and ended the server call; final server state is `ended`. Aliyun backend timeout deployment remains separate from this current debug-build evidence. |
| 5.5-EV-020 | Protected route smoke | Settings / Edit Profile | `docs/version_plans/assets/5.5/screenshots/settings_smoke.png`; `docs/version_plans/assets/5.5/screenshots/edit_profile_smoke.png` | XML: `docs/version_plans/assets/5.5/xml/settings_smoke.xml`, `docs/version_plans/assets/5.5/xml/edit_profile_smoke.xml`; ADB direct-route smoke | Emulator `0.05.04 / 50400` | Passed | Settings opened from Profile and showed setting groups such as display, appearance, performance, privacy, and version-related content without crash or auth redirect. Edit Profile opened through `elitesync_initial_route=/profile/edit` and showed `编辑资料`, server-truth explanation, nickname, gender, birthday, birth time, birth place, city, and relationship-goal fields without crash, red screen, cast error, or infinite skeleton. No save/write action was performed. |
| 5.5-EV-021 | 5.5-FB-006 | Aliyun RTC invite timeout deploy parity | N/A | Local `php artisan test --filter=RtcApiTest`; SSH grep on `/opt/elitesync/services/backend-laravel/app/Services/RtcSessionService.php`; `systemctl is-active`; `/up` health check | Aliyun backend `101.133.161.203` | Passed / code deploy parity verified | Local `RtcApiTest` passed with 8 deprecation notices. The Aliyun backend was deployed by backing up and uploading only `RtcSessionService.php`, then running Laravel cache clear/cache and restarting `php8.4-fpm`. Remote grep now shows `INVITE_TIMEOUT_SECONDS = 30`; `php8.4-fpm`, `nginx`, and `elitesync-ws` are active; local health check returns HTTP 200. No migration, seeder, backup restore, DB overwrite, or remote create/end call write verification was performed in this evidence step. |
| 5.5-EV-022 | 5.5-FB-006 | Aliyun RTC write-style retest | N/A | Remote API login / create / list / accept / connect / heartbeat / end sequence; `call_id: 116`; pre-test DB backup `D:\EliteSync_Aliyun_DB_Backups\20260508_142630` | Aliyun backend `101.133.161.203` | Passed / remote write loop closed | After user approval and a verified Aliyun MySQL backup, the smoke accounts `17094346566` and `13772423130` created voice `call_id: 116`. The create response started as `calling` and measured `expires_at - ringing_at` at exactly `30` seconds before connect; after connect, the session expiry was extended for call lifetime. The receiver `/api/v1/rtc/calls` list saw the call, accept returned `connecting`, connect returned `in_call`, both caller and receiver heartbeat calls succeeded, and final state was `ended` / terminal with events `created,accepted,connected,heartbeat,heartbeat,ended`. This verifies remote RTC create/end write behavior for the deployed 30s invite timeout. It does not prove restore drill, migration safety, queue logs, or non-RTC Cloud DB health. |
| 5.5-EV-023 | Cloud read-only / ops checks | Aliyun backend / DB / queue / logs | N/A | SSH read-only checks: `php artisan migrate:status --no-interaction`; aggregate `SELECT` counts; `systemctl is-active`; `/up`; version API; Laravel and websocket log tails | Aliyun backend `101.133.161.203` | Passed with observations | Read-only checks found migration table count `49`, latest migration `2026_04_24_000020_add_last_seen_at_to_rtc_sessions_table`, users `4`, rtc_sessions `116`, rtc_session_events `1928`, notifications `229`, jobs `0`, and services `php8.4-fpm`, `nginx`, `elitesync-ws` all active. `/up` returned HTTP 200 and version API still reports `0.05.04 / 50400`. No same-day Laravel error-like lines were found in the checked tail. Observation: remote `php artisan about` reports environment `local` and debug mode enabled; treat this as a separate ops-hardening item, not a 5.5 runtime fix. Restore drill was not performed. |
| 5.5-EV-024 | Backend / Flutter regression | Local test suite | N/A | `php artisan test --filter='RtcApiTest|AppVersionApiTest|NotificationApiTest|StatusPostApiTest|MessageApiTest|MediaUploadEndpointTest|MatchPayloadContractTest'`; `flutter test ...status_post_entity_test.dart ...status_author_entity_test.dart ...admin_dashboard_page_test.dart`; `flutter analyze` | Local workspace | Mixed: tests passed, analyze has existing infos | The focused backend regression now passes after fixing test `APP_URL` in `phpunit.xml`; remaining output is 28 PHP 8.5 PDO deprecation notices. Focused Flutter tests for 5.5 status parsing and admin dashboard pass. `flutter analyze` still exits non-zero on 14 existing info-level lint findings such as `avoid_print`, null-aware suggestions, and string-interpolation style; no new runtime failure was identified in this pass. |
| 5.5-EV-025 | 5.5-FB-006 | RTC terminal incoming-call UI | `.tmp/5_5_device_smoke/phone_incoming_116_after_fix.png` | XML `.tmp/5_5_device_smoke/phone_incoming_116_after_fix.xml`; direct route `/rtc/incoming/116`; focused RTC Flutter tests | Phone `TG9L8HOBKFMJZTZX` and emulator `emulator-5554`, both installed rebuilt debug APK `0.05.04 / 50400` | Passed / stale terminal actions removed | Dual devices were online and both installed the rebuilt debug APK. Before the fix, the phone could relaunch into ended `call_id: 116` as `语音来电` while still showing `接听` / `拒绝`. The page now renders terminal status `通话结束` with `来电操作: 通话已收口 / 查看结果`, exposes `查看结果` and `返回`, and no longer exposes accept / reject actions for the ended call. Focused RTC page tests passed after adding terminal-call coverage and stabilizing the navigation-back test scroll. |

## 3. Historical Reference Boundary

Historical 5.4 evidence remains useful for protected-surface comparison only:

- `docs/version_plans/5.4_OBSERVABILITY_EVIDENCE_INDEX.md`
- `docs/version_plans/assets/5.4/screenshots/`
- `docs/version_plans/assets/5.4/xml/`

The old `0.04.09 / 40900` Version Center evidence is historical and must not be used as current 5.5 release-chain evidence.
