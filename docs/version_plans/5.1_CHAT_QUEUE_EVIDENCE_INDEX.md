# EliteSync 5.1B Chat Queue Evidence Index

更新时间：2026-05-01

## 证据状态

当前为实现记录索引，正式截图尚未采集。

## 待采集截图

建议目录：

`docs/version_plans/assets/5.1/chat/`

待采集：

- `chat_queue_continue.png`：Chat 页显示 `继续聊` 建议。
- `chat_queue_return.png`：Chat 页显示 `回聊` 建议。
- `chat_queue_recovery.png`：Chat 页显示 `冷场恢复` 建议。
- `chat_queue_draft_inserted.png`：点击建议后只写入输入框草稿。

## 机器证据

已执行验证：

- `flutter test test/features/chat/presentation/pages/chat_room_page_test.dart`
- `flutter test test/features/chat/presentation/widgets/message_input_bar_test.dart`
- `flutter test test/features/chat/data/dto/message_dto_test.dart`
- `flutter test test/features/chat/data/datasource/chat_socket_data_source_test.dart`

结果：通过。

静态检查：

- `flutter analyze`

结果：未发现本轮新增 error / warning；仓库现有 19 条 analyzer info 仍存在，主要为既有 `avoid_print` 与样式提示。

## 验收注意

- 截图必须来自当前包。
- 文件名、截图内容、页面实际内容必须一致。
- 证据不得沿用 5.0 截图。
- 点击建议后不得出现“已发送”之类伪成功反馈。
