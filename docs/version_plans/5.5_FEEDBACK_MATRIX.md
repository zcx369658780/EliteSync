# EliteSync 5.5 Feedback Matrix

Updated: 2026-05-08

Purpose: track real small-sample feedback, implementation status, and acceptance status for 5.5.

Rules:

- Do not invent feedback.
- Every P0 / P1 item must point to a concrete page, path, screenshot, XML, log, user quote, or reproduced walkthrough.
- Do not treat generic Soul parity, advisor preference, or a single subjective impression as a structural product problem.
- Cross-layer issues must become blocker reports before implementation.
- Database, migration, backup, restore, queue, and RTC success claims remain observations unless current approved evidence exists. As of 2026-05-08, 5.5 has current approved evidence for Cloud DB read-only audit, backup existence, migration-level read-only checks, queue / log tail checks, and RTC success; restore drill remains unperformed.

## 1. Feedback Source Freeze

Formal sources:

- Real user / tester feedback supplied by the user.
- Current `0.05.04 / 50400` app walkthrough evidence.
- Screenshots and XML captured from the current installed host package.
- Read-only health / version / log evidence when directly tied to a feedback item.

Reference-only sources:

- Historical 5.4 screenshots and XML.
- GPT / advisor suggestions without concrete path evidence.
- Competitor analysis not tied to current EliteSync feedback.

Excluded from 5.5 feedback:

- `services/backend-laravel/config/matching.php` matching marker semantics.
- Release-chain / version bump work.
- Cloud DB / backup / restore / migration execution.

## 2. Matrix

| ID | Source | Module | Page / Path | Feedback | Evidence | Severity | Suspected Layer | Proposed Action | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 5.5-FB-001 | User supplied screenshot + Claude Appium test + user confirmation | Status / Plaza | Status Plaza / publish status | Publishing a status fails with runtime error: `type '_Map<String, dynamic>' is not a subtype of type 'List<dynamic>?' in type cast`. The page shows a red failure snackbar after the user attempts to publish. | User supplied Image #1 and Claude UI precheck in current session | P0 blocker | Flutter response parsing compatibility | Fixed the smallest Flutter parsing issue by making `StatusPostEntity` tolerate map-shaped `public_personality` and `media` payloads. Focused test and analyze passed; rebuilt and installed debug APK. Claude UI precheck passed; user confirmed first-round result. | fixed / user accepted |
| 5.5-FB-002 | User supplied screenshot + Claude Appium test | Match | Match detail | Match detail page opens but remains in a near-empty loading / skeleton state with no actionable content. | User supplied Image #2 and Claude Appium walkthrough in current session | P1 high friction | Active match data availability / no-match error state | Claude found the detail path now resolves to a clear `详情加载失败` / `no match statusCode=404` after refresh, so it is not an infinite skeleton. Success-state rendering still needs active match data after Drop. | data-dependent observation |
| 5.5-FB-003 | User supplied screenshot + Claude Appium test | Me / Profile | Me page / personal operating area | Floating `发布状态` action overlaps the lower personal operating area and competes with bottom navigation, making the page feel obstructed. | User supplied Image #3 and Claude screenshots in current session | P2 medium friction | Flutter shell center action scope | Claude confirmed the floating action is visible across top/middle/bottom profile scroll positions and competes with profile content and nav. Fixed by scoping the shell `发布状态` center action to the Home tab only. Claude regression confirmed Profile no longer shows the floating action and Home still keeps the entry. | fixed / Claude verified |
| 5.5-FB-004 | User supplied screenshot + Claude Appium test | Astro / Profile | Astro Overview / Ziwei preview | Ziwei overview preview clips the bottom row of the 4x4 palace grid; palace labels such as `福德` and `疾厄` are only partially visible. | User supplied Image #4 and Claude Appium structure check in current session | P2 medium friction | Flutter layout / fixed preview height | Fixed the preview height to derive from the available width, 4 columns, and 4 rows so the complete palace grid is visible without changing astro truth data. Claude structural smoke reported pass. | fixed / Claude structural pass |
| 5.5-FB-005 | Claude Appium test | Status / Author Profile | Status Plaza -> author profile | Opening an author profile from Status Plaza shows `作者资料加载失败` with `type '_Map<String, dynamic>' is not a subtype of type 'List<dynamic>?' in type cast`. | Claude Appium retest in current session | P1 high friction | Flutter status author parsing compatibility | Fixed `StatusAuthorEntity` to tolerate map-shaped `public_personality` payloads, matching the compatibility added to status posts. Claude regression confirmed author profile now renders without the cast error. | fixed / Claude verified |
| 5.5-FB-006 | ADB dual-device/API-assisted RTC test | RTC / Calls | Receiver-side incoming call watcher | Phone receiver did not surface an incoming-call UI before timeout even though the receiver account could see the active `/api/v1/rtc/calls` item. Logcat showed `RTC_INVITE_PROVIDER_ERROR NoSuchMethodError: AsyncData<SessionState> has no instance method maybeWhen`, so the watcher failed before scanning calls. Later dual-device retest also found ended `call_id: 116` could reopen the incoming-call page while still showing `接听` / `拒绝`. | ADB logcat, `/api/v1/rtc/calls` API check, phone UI XML, app-to-app `call_id: 115`, Aliyun write-style `call_id: 116`, and terminal incoming-call XML in current session | P1 high friction | Flutter RTC watcher session-state read / backend invite timeout / terminal incoming-call UI | Fixed `_currentUserId` to read typed `AsyncData<SessionState>` safely, moved incoming-call scan ahead of notification fetch, lengthened backend invite timeout from 10s to 30s, deployed the timeout change to Aliyun, verified remote write-style create / accept / connect / heartbeat / end through `call_id: 116`, and changed `RtcIncomingCallPage` to replace accept / reject with `查看结果` / `返回` when `session.isTerminal`. Phone receiver reached `in_call` for API-assisted `call_id: 114`; app-to-app `call_id: 115` verified two-sided LiveKit media; terminal UI was verified on phone direct route `/rtc/incoming/116`. | fixed / RTC verified with observations |

## 3. Severity Rules

- P0 blocker: cannot complete a core flow, data truth is at risk, or page success state conflicts with backend / cloud / log evidence.
- P1 high friction: user can proceed but misunderstanding or failed recovery is likely.
- P2 medium friction: wording, density, order, or weak-state clarity issue that affects confidence but not completion.
- observation: useful note that should not drive implementation by itself.

## 4. First Runtime Slice Gate

No runtime slice is allowed until at least one feedback row has:

- concrete page / path;
- evidence or user-provided feedback text;
- severity;
- suspected layer;
- smallest proposed action;
- explicit non-goals.
