# EliteSync 6.0-A1 Staging / Production Split Readonly Verification Prepackage

## 0. 当前结论

- 本文是 Candidate C 的 authorization / preparation document。
- 本文不是 Candidate C implementation。
- 本文不代表 Candidate C 已获实施授权。
- 本文不代表 Candidate C 已实施。
- 本文不代表 staging verification passed。
- 本文不代表 production verification passed。
- 本文不代表 API smoke passed。
- 本文不代表 A1 final acceptance。
- 本文不代表 production ready。
- 本文不代表 full v2 skeleton complete。
- 本文不代表 A2 start。
- 本文只用于提交 GPT 顾问审查，决定是否允许未来执行极窄只读环境分离验证。

## 1. 当前基线

- 当前分支：`feature/5.0-alpha-readiness-20260501`。
- 当前 HEAD 以 `git rev-parse HEAD` / `git log` 实时结果为准，不把本文所在提交写成“当前最新 HEAD”。
- R1 runtime commit：`90436e2d17c611907dfe4322135c7e4ba0bbb23d feat: add readonly v2 runtime slice`。
- R1 stage acceptance package commit：`99cfd0ba47186bbacc03770b2679afe558a9a5f8 docs: add 6.0-A1 R1 stage acceptance package`。
- R1 status sync commit：`8670976a223d85236c6695aa4912729f55cb3149 docs: sync 6.0-A1 R1 stage acceptance status`。
- R2 authorization prepackage commit：`e1627b8dd9f4ec6967f9c9940e13e6cb788895ff docs: add 6.0-A1 R2 authorization prepackage`。
- R2 prepackage current-docs sync commit：`e54c543fc8c5fd7a8a8ea9728cc556093cf1dc1e docs: sync 6.0-A1 R2 prepackage status`。
- R1 已完成 endpoint：
  - `GET api/v2/app/health`
  - `GET api/v2/app/readiness`
  - `GET api/v2/contracts/location`
- R1 不包含范围：
  - 不包含 authenticated v2 endpoint。
  - 不包含 profile display runtime。
  - 不包含 location context runtime。
  - 不包含 Date Drop / buddy runtime。
  - 不包含 Flutter integration。
  - 不包含 production release。
  - 不包含 DB / migration / write smoke。
- R2 prepackage 顾问结论：GPT 顾问已验收 R2 authorization prepackage，并建议当前 reject endpoint R2 for now，下一步方向选择 staging / production split readonly verification package。
- Candidate C 尚未实施。

## 2. Candidate C 目标

Candidate C 的目标是准备一个极窄、只读、可审查、可回滚的 staging / production split verification 计划，用来确认：

- production 默认继续 v1。
- Flutter 默认不切 v2。
- v2 仅保持 R1 readonly endpoints。
- staging / production base URL 与环境边界被文档化。
- 不执行真实用户写操作。
- 不执行 DB / migration。
- 不执行 release chain。
- 不触碰 Date Drop / buddy / Flutter integration。

Candidate C 只作为 A1 内的环境分离只读验证准备方向，不扩大 endpoint，不替代 R2 runtime authorization，也不改变 production / Flutter 默认行为。

## 3. 本包允许范围

本包只允许规划未来验证，不执行验证。

允许规划：

- future route-list verification plan。
- future readonly endpoint availability plan。
- future environment config source audit plan。
- future no-production-default-traffic evidence plan。
- future no-Flutter-v2-default evidence plan。
- future no-DB-write / no-migration evidence plan。
- future rollback / stop condition plan。
- future evidence index structure。
- future export path policy。

未来所有导出文件默认放在：

`C:\Users\zcxve\Downloads\`

不得默认导出到 Desktop / TEMP。

## 4. 本包禁止范围

- 不执行 API / smoke。
- 不请求 staging。
- 不请求 production。
- 不执行 route-list。
- 不执行 artisan。
- 不执行 PHPUnit。
- 不执行 composer。
- 不执行 migration。
- 不修改 `.env` / config。
- 不读取或输出 secrets。
- 不修改 Flutter。
- 不切 base URL。
- 不修改 release chain。
- 不新增 route / controller / DTO / Resource。
- 不修改 R1 runtime。
- 不进入 A2 / Date Drop / buddy / production release。

## 5. Future verification candidates

未来若获授权，可分为以下候选步骤，但本文不得执行。

### A. Local / repo-only readonly environment split audit

- 只读查看配置文件名、文档、示例 env、base URL 配置来源。
- 不输出 secret 值。
- 不修改 config。
- 不执行 artisan / composer / migration。
- 不请求 staging / production。

### B. Staging readonly metadata verification

- 仅在用户明确授权后执行。
- 只请求 staging。
- 只验证 health / readiness / location contract 或环境 metadata。
- 不写 DB。
- 不登录真实用户。
- 不访问 production。
- 不修改 staging config。

### C. Production readonly metadata verification

- 仅在用户明确授权后执行。
- 只请求 production 的低风险 readonly metadata / health 类 endpoint。
- 不请求用户数据。
- 不触发 write smoke。
- 不改变 production traffic。
- 不修改 production config。
- 不修改 release chain。

### D. Flutter default-v1 verification

- 只读确认 Flutter 默认 base URL / feature flag 仍不切 v2。
- 不修改 Flutter。
- 不 build APK。
- 不 release。
- 不切 production 默认流量。

## 6. Required stop conditions

以下任一情况出现时，必须停止等待用户确认：

- 需要读取或输出 secret。
- 需要修改 `.env` / config / deployment。
- 需要请求 production。
- 需要请求 staging。
- 需要执行 artisan / PHPUnit / composer / migration。
- 需要写 DB。
- 需要真实用户登录或 token。
- 需要 Flutter 修改或 build。
- 需要 release chain 修改。
- 需要新增 endpoint。
- 发现 v2 route 需要 auth / session / cache / config 改动。
- 发现 Candidate C 无法保持 readonly。

## 7. Future evidence plan

以下仅规划未来证据，不得在本文阶段执行：

- git baseline evidence：记录 branch、HEAD、status、recent log。
- file diff evidence：确认只变更获授权的文档或未来获授权的 evidence 文件。
- route-list evidence：仅在未来单独授权后执行，并限定为只读输出。
- php -l evidence：仅在未来单独授权后执行，并限定获授权文件范围。
- targeted test evidence only if separately authorized。
- no production request evidence：在未授权前必须证明没有 production request。
- no staging request evidence unless authorized。
- no DB write / no migration evidence。
- no Flutter change evidence。
- no release chain change evidence。
- export artifact SHA256 evidence。
- final status summary。

## 8. Export artifact policy

- 从本阶段开始，所有 Codex 导出目录默认位于：

`C:\Users\zcxve\Downloads\`

- 推荐格式：

`C:\Users\zcxve\Downloads\elitesync_<task_name>_<YYYY_MM_DD>`

- 不再默认使用 Desktop。
- 不再默认使用 TEMP。
- 不使用通配符清理导出目录。
- 删除旧导出目录必须使用精确路径和用户授权。

## 9. GPT advisor decision request

请求 GPT 顾问选择：

- Approve future Candidate C local/repo-only readonly audit prompt。
- Approve future Candidate C staging readonly verification prompt。
- Approve future Candidate C production readonly verification prompt。
- Request Claude horizontal review before any staging / production request。
- Reject Candidate C and return to A1 final-gate documentation only。

如果 GPT 顾问批准，也只代表下一条 prompt 可继续准备，不代表自动允许 staging / production 请求。
