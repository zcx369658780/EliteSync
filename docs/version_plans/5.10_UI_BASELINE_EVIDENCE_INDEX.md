# EliteSync 5.10 UI Baseline Evidence Index

更新时间：2026-05-12

## 1. Evidence Scope

5.10 UI 证据目标是证明 Settings / 用户控制入口新增解释层治理与用户控制区，并保持原 Settings 主流程可见。

核心文本：

- `解释与建议设置`
- `关系解释提示`
- `个人表达建议`
- `聊天开场建议`
- `本机控制 · 后续完善`
- `不会写入资料`
- `不会改变星盘或匹配算法`
- `不会自动发送消息`
- `关闭后仍可使用主流程`
- `了解这些建议如何工作`
- `账号与安全`
- `修改密码`
- `版本中心`

## 2. Automated UI Evidence

Widget/page evidence：

- `apps/flutter_elitesync_module/test/features/profile/presentation/pages/settings_page_test.dart`

通过命令：

```powershell
cd apps/flutter_elitesync_module
flutter test test/features/profile/presentation/pages/settings_page_test.dart
```

结果：

```text
All tests passed!
```

Targeted analyze：

```powershell
flutter analyze lib/features/profile/presentation/pages/settings_page.dart test/features/profile/presentation/pages/settings_page_test.dart
```

结果：

```text
No issues found!
```

覆盖：

- `解释与建议设置` 可见。
- 三类解释层说明可见。
- 免责声明可见。
- `了解这些建议如何工作` 可打开说明。
- `账号与安全`、`修改密码` 等原 Settings 入口仍可见。

## 3. Runtime Evidence

已安装并启动 Flutter debug host：

```powershell
flutter run -d emulator-5554 -t lib/main_dev.dart --dart-define=ELITESYNC_INITIAL_ROUTE=/profile/settings --no-resident
```

设备：

```text
emulator-5554
```

运行态证据文件：

- `docs/version_plans/assets/5.10/settings/settings_top.png`
- `docs/version_plans/assets/5.10/settings/settings_top.xml`
- `docs/version_plans/assets/5.10/settings/explanation_control_card.png`
- `docs/version_plans/assets/5.10/settings/explanation_control_card.xml`
- `docs/version_plans/assets/5.10/settings/explanation_control_dialog.png`
- `docs/version_plans/assets/5.10/settings/explanation_control_dialog.xml`

结论：

- App 已在 emulator 启动。
- 当前已有登录 session，可通过底部 `我的` 进入 Profile，再进入 Settings。
- `settings_top.xml` 可读到 `设置中心`、`设置口径`、`显示与外观`、`资料与显示`。
- `explanation_control_card.xml` 可读到 `解释与建议设置`、`关系解释提示`、`个人表达建议`、`聊天开场建议`、`不会写入资料`、`不会改变星盘或匹配算法`、`不会自动发送消息`、`关闭后仍可使用主流程`、`了解这些建议如何工作`、`账号与安全` 和 `修改密码`。
- `explanation_control_dialog.xml` 可读到 `这些建议如何工作`、`不会读取私密聊天`、`不会写入资料`、`不会改变星盘或匹配算法`、`不会自动发送消息`、`关闭后仍可使用匹配、个人资料和聊天主流程`。

## 4. Evidence Interpretation

本轮 UI 证据口径：

- 自动化 widget/page test 证明 Settings 解释层治理入口可渲染，详情说明可打开，原账号入口仍可见。
- runtime screenshot/XML 证明更新后的 debug host 中 Settings 页面可达，5.10 解释与建议设置实际进入设置主滚动区。
- 详情 sheet 证明来源、不做项和用户控制说明可读。
- 本轮不发布 APK，不把 debug host 证据写成 release 证据。

## 5. Protected Surface Evidence

命令：

```powershell
git status --short -- services database routes config scripts/release_android_update_aliyun.ps1 apps/android/app/build.gradle.kts
```

结果：空。

说明：5.10 UI 证据收口没有触碰 backend / DB / API / release chain。
