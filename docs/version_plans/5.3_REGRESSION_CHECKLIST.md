# EliteSync 5.3 Regression Checklist

Updated: 2026-05-02

## 1. Scope

- Version under check: `5.3` 功能覆盖收尾版
- Main handoff: `docs/version_plans/5.3_HANDOFF_MASTER.md`
- Evidence index: `docs/version_plans/5.3_UI_BASELINE_EVIDENCE_INDEX.md`
- Capture package: current installed debug package on `emulator-5554`

## 2. New Capability Checks

### Discover

- Search state is visible.
- Local recommendation reason state is visible.
- Long-press action sheet is visible.
- Actions remain local guidance, not server truth rewrite.

### Chat

- Chat list guidance card is visible.
- Chat list flow guidance sheet is visible.
- Relationship summary is visible in chat room.
- AI continuation / low-pressure suggestion is visible.
- Cold recovery and voice rhythm guidance are visible.

### Me / Appearance

- Personal operating hub is visible.
- Tag expression system is visible.
- AI display advice is visible.
- AI draft helper and its sheet are visible.
- Light voice candidate slot is visible.
- Personal appearance preview layer is visible.

### Settings

- Appearance boundary entry is visible.
- Appearance boundary sheet is visible.
- Version center remains reachable.
- Version center still shows `0.04.09 / 40900`.

## 3. Protected Surface Regression

- Messages page remains reachable.
- Chat room page remains reachable.
- Settings page remains reachable.
- Profile edit entry remains reachable from the operating hub.

## 4. Observation Carryover

- A global `发布状态` floating CTA can still cover part of the lower sheet content area in some screens.
- Some light-voice dialog copy still carries older `5.2` wording.
- `稍后再聊` remains a local guidance position, not a persisted queue.
- `冷场恢复` remains a low-pressure re-entry helper, not an auto-resume system.
- `AI 续话` remains a local draft helper, not a server-side send pipeline.
- `个人空间外观` remains a preview layer, not a persistent dressing system.
- Discover recommendation explanations remain local explanation surfaces.
- Settings appearance entry remains a boundary explainer, not a new full settings center.

## 5. Evidence Rules

- Evidence must come from the current 5.3 installed package.
- Screenshot file name, screenshot content, and actual page must match.
- Do not reuse 5.2 or earlier screenshots as 5.3 evidence.
- If a capture and its label disagree, fix the evidence label/index first.

## 6. Result

- 5.3 regression evidence is sufficient for `pass with observations`.
- No backend contract, truth chain, or release chain changes are implied by this checklist.
