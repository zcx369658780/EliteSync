# EliteSync 6.0-A1 Option B Sites-enabled Symlink Authorization Package

## 0. 当前结论

- 本文是 Option B sites-enabled symlink authorization package。
- 本文不是 symlink execution result。
- 本文不代表 sites-enabled symlink 已创建。
- 本文不代表 Nginx reload / restart 已执行。
- 本文不代表 IP staging 已启用。
- 本文不代表 staging reachable。
- 本文不代表 staging verification passed。
- 本文不代表 production verification passed。
- 本文不代表 API smoke passed。
- 本文不代表 Candidate C implementation / completed。
- 本文不代表 A1 final acceptance / production ready / full v2 skeleton complete / A2 start。
- 本文只用于提交 GPT 顾问和用户审查，决定未来是否允许创建 sites-enabled symlink 并执行 nginx -t。

## 1. 当前基线

| 项 | 当前记录 |
|---|---|
| 当前分支 | `feature/5.0-alpha-readiness-20260501` |
| 当前 HEAD | `9c882376a1189977d8ae86818eefd40efc2fcfc0` |
| 当前 worktree clean 状态 | clean after restoring tracked handoff file before this package creation |
| 当前 remote-published scaffold report | `docs/version_plans/6.0_A1_IP_STAGING_OPTION_B_NARROW_SCAFFOLD_PREFLIGHT_REPORT.md` |
| scaffold report commit | `9c882376a1189977d8ae86818eefd40efc2fcfc0` |

当前服务器侧已知事实：

- `.env.staging` 已从 `.env.example` 创建。
- `/etc/nginx/sites-available/elitesync_staging_ip.conf` 已创建。
- staging draft bind only `127.0.0.1:8088`。
- 8088 创建前未占用。
- php8.4-fpm.sock discovered and used。
- 当前未创建 sites-enabled symlink。
- 当前未 reload / restart Nginx。
- 当前未执行 endpoint verification。
- 当前 staging 不可访问。

## 2. Future symlink execution goal

未来若用户另行明确授权，本阶段只允许：

- SSH 到授权服务器。
- 确认 worktree clean。
- 确认 backup directory 存在。
- 确认 staging draft 文件存在。
- 确认 sites-enabled 尚无同名 symlink。
- 创建 symlink：
  `/etc/nginx/sites-enabled/elitesync_staging_ip.conf -> /etc/nginx/sites-available/elitesync_staging_ip.conf`
- 执行 `nginx -t`。
- 若 `nginx -t` 有 warning 或 failure，停止。
- 不 reload。
- 不 request endpoint。

## 3. Future allowed scope

未来若另行授权 symlink execution，只允许：

- `ln -s` 创建上述 symlink。
- `nginx -t`。
- 记录 symlink state。
- 记录 `nginx -t` result。
- 不输出 Nginx config body。
- 不读取 `.env / .env.*`。
- 不输出 secrets。

## 4. Future forbidden scope

即使未来授权 symlink execution，仍禁止：

- nginx reload / restart。
- systemctl reload / restart nginx。
- 修改 `/etc/nginx/sites-available/elitesync.conf`。
- 修改 production/default server block。
- 修改 security group / firewall。
- 请求 `http://127.0.0.1:8088`。
- 建立 SSH tunnel。
- 执行 endpoint verification。
- 请求 `slowdate.top`。
- 请求 `101.133.161.203` 根路径。
- 创建 staging DB / Redis DB index / storage path。
- 读取 / 输出 `.env` 内容。
- 输出 secrets。
- 执行 API / smoke / artisan / PHPUnit / composer / migration。
- 修改 Android / Flutter release chain。

## 5. Stop conditions

未来 symlink execution 遇到以下情况必须停止：

- worktree 不 clean。
- SSH target mismatch。
- backup directory missing。
- staging draft file missing。
- symlink already exists。
- symlink target mismatch。
- `nginx -t` warning。
- `nginx -t` failure。
- `nginx -t` output contains unexpected production/default block warning。
- symlink creation would touch production/default config。
- reload becomes necessary。
- endpoint verification becomes necessary。
- any command would read `.env` content。
- any command would output secrets。

## 6. Evidence package plan

未来 symlink execution report 应包含：

- branch / HEAD / worktree。
- SSH host/user summary。
- backup reference confirmation。
- staging draft file existence。
- symlink created yes/no。
- symlink target。
- `nginx -t` result。
- reload executed must be no。
- endpoint verification executed must be no。
- no-secret confirmation。
- final recommendation。

## 7. Rollback note

- 如果 symlink 创建后 `nginx -t` warning / fail，应删除 newly created symlink。
- 删除 symlink 后再次确认 sites-enabled state。
- 不 reload。
- 不 touch production config。
- 不 delete sites-available draft unless separately authorized。
- rollback report must be redacted。

## 8. Recommended GPT advisor decision

建议选项只能是：

- approve sites-enabled symlink execution only。
- request redacted Nginx draft review first。
- keep scaffold disabled for now。
- needs Claude re-review。
- reject symlink execution for now。

## 9. Overclaim guard

明确不能写：

- IP staging enabled
- staging reachable
- staging verification passed
- production verification passed
- API smoke passed
- Candidate C implemented
- Candidate C completed
- A1 final acceptance complete
- production ready
- full v2 skeleton complete
- A2 start

正确写法：

- symlink authorization package is ready。
- no symlink has been created in this task。
- no Nginx reload has been executed。
- no endpoint verification has been executed。
- staging remains disabled until symlink and reload are separately authorized and completed。
