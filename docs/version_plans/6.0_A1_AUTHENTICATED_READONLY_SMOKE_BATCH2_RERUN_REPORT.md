# EliteSync 6.0-A1 Authenticated Readonly Smoke Batch 2 Rerun Report

执行时间：2026-05-14 16:51:06 +08:00  
当前阶段：A1 authenticated readonly smoke execution - Batch 2 rerun  
授权类型：production-readonly-explicit + authenticated readonly only  
BASE_URL：`http://101.133.161.203`  
报告定位：authenticated readonly smoke Batch 2 重跑执行报告。本轮只执行用户授权的 4 个 authenticated readonly GET。

## 0. 执行边界

- 本轮只执行以下 4 个 authenticated readonly GET：
  - `GET /api/v1/profile/basic`
  - `GET /api/v1/profile/astro/summary`
  - `GET /api/v1/questionnaire/history`
  - `GET /api/v1/notifications/unread-count`
- 本轮未执行任何 POST。
- 本轮未执行 `GET /api/v1/profile/astro/chart`。
- 本轮未执行 `GET /api/v1/messages`。
- 本轮未执行 `GET /api/v1/media/{assetId}`。
- 本轮未执行 `GET /api/v1/rtc/calls/{callId}/livekit`。
- 本轮未获取、生成、保存或回显 token。
- 本轮未使用真实用户账号。
- 本轮未写 DB，未执行 migration，未修改 production 配置。
- 本轮未修改 `apps/`、`services/`、`database/`、`scripts/` 下任何 runtime 或 release 文件。
- 本轮未运行 release script，未生成 APK，未进入 v2 skeleton runtime。

## 1. Git 与环境状态

| 项 | 结果 |
| --- | --- |
| 当前分支 | `feature/5.0-alpha-readiness-20260501` |
| 当前 HEAD | `fc478bdc` |
| git status 执行前 | clean |
| git status 执行后 | 仅新增本报告文件 |
| BASE_URL | `http://101.133.161.203` |
| 授权类型 | `production-readonly-explicit + authenticated readonly only` |
| `ELITESYNC_TOKEN_A` | set，未回显 token |

## 2. Token 处理结果

| 项 | 结果 |
| --- | --- |
| token 是否请求 | no |
| token 是否生成 | no |
| token 是否保存 | no |
| token 是否回显 | no |
| token 是否写入文件 | no |
| token 是否写入日志 / git / 截图 | no |

说明：本轮只使用执行前已临时设置的 `ELITESYNC_TOKEN_A` 作为 Authorization header；报告不包含 token 原文。

## 3. Smoke 结果总表

| Smoke ID | 名称 | Endpoint | 结果 | HTTP status | Auth required | 是否写 DB | 响应字段摘要 | Observation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AUTH-RO-01 | profile basic GET | `/api/v1/profile/basic` | pass | 200 | yes | no | 返回 profile 顶层字段；手机号、生日 / 出生时间、出生经纬度字段存在但已脱敏；未输出具体值 | 响应包含手机号字段和值，但报告未输出具体值 |
| AUTH-RO-02 | astro summary GET | `/api/v1/profile/astro/summary` | pass | 200 | yes | no | 返回 `exists` 与 `profile`；birth time、birth lat/lng、position_signature、完整命盘 / 玄学细节字段存在但已脱敏；未输出具体值 | v1 contract 仍返回位置与工程字段，继续作为 A1 位置展示清理 observation |
| AUTH-RO-03 | questionnaire history GET | `/api/v1/questionnaire/history` | pass | 200 | yes | no | 返回 `items` 与 `total`；items count = 2；未输出完整答案 | none |
| AUTH-RO-04 | notifications unread-count GET | `/api/v1/notifications/unread-count` | pass | 200 | yes | no | 返回 `ok` 与 `unread_total` 字段；未输出 notification body | none |

## 4. 响应字段摘要

### AUTH-RO-01 profile basic GET

- HTTP status：200。
- 顶层字段存在：id、name、phone、birthday、birth_time、zodiac_animal、gender、city、relationship_goal、birth_place、birth_lat、birth_lng、moderation_status、moderation_note、realname_verified。
- 隐私字段处理：
  - phone 字段存在，具体值已脱敏，未输出。
  - birthday / birth_time 字段存在，具体值已脱敏，未输出。
  - birth_lat / birth_lng 字段存在，具体值已脱敏，未输出。
- 判定：pass。

### AUTH-RO-02 astro summary GET

- HTTP status：200。
- 顶层字段存在：exists、profile。
- profile 字段摘要：birthday、birth_time、birth_place、birth_lat、birth_lng、sun_sign、moon_sign、asc_sign、bazi、true_solar_time、location_shift_minutes、longitude_offset_minutes、equation_of_time_minutes、position_signature、location_source、da_yun、liu_nian、wu_xing、ziwei、notes、accuracy、confidence、western_engine、western_precision、western_confidence、western_degraded、western_degrade_reason、western_rollout_enabled、western_rollout_reason、computed_at、route_mode、metadata。
- 隐私 / 工程字段处理：
  - birth_time、birth_lat、birth_lng、longitude_offset_minutes、position_signature 等字段存在，具体值已脱敏，未输出。
  - bazi、ziwei、natal/chart-like 玄学细节字段只记录存在性，未输出完整内容。
- 判定：pass。

### AUTH-RO-03 questionnaire history GET

- HTTP status：200。
- 顶层字段存在：items、total。
- items count：2。
- total 字段存在。
- 完整问卷答案未输出。
- 判定：pass。

### AUTH-RO-04 notifications unread-count GET

- HTTP status：200。
- 顶层字段存在：ok、unread_total。
- notification body 未输出。
- 判定：pass。

## 5. 隐私与保护面确认

| 项 | 结果 |
| --- | --- |
| 是否发现 token 泄露 | no |
| 是否发现手机号直接输出 | no |
| 是否发现密码泄露 | no |
| 是否发现私聊正文泄露 | no |
| 是否发现完整出生时间直接输出 | no |
| 是否发现精确经纬度直接输出 | no |
| 是否发现 DB host / Redis host / OSS key / SSH key / `.env` secret 泄露 | no |
| 是否发现 LiveKit token / secret 泄露 | no |
| 是否发现真实用户身份信息直接输出 | no |

Observation：

- profile/basic 与 astro summary 的 v1 contract 仍返回手机号字段、出生时间字段、出生经纬度字段或位置 / engine 相关字段。本报告只记录字段存在性并脱敏，不输出具体值。
- 该 observation 与 v1 contract audit 中的位置展示 / profile truth 保护面一致，不阻断本轮 authenticated readonly smoke，但后续位置展示清理仍需继续承接。

## 6. 执行保护面确认

| 项 | 结果 |
| --- | --- |
| 是否请求 token | no |
| 是否回显 token | no |
| 是否写入 token 到文件 | no |
| 是否写 DB | no |
| 是否执行 migration | no |
| 是否修改 runtime | no |
| 是否修改 `.env` | no |
| 是否修改 release chain | no |
| 是否运行 release script | no |
| 是否生成 APK | no |
| 是否执行 write smoke | no |
| 是否进入 v2 skeleton | no |

## 7. 判定

| 项 | 判定 |
| --- | --- |
| AUTH-RO-01 profile basic | pass |
| AUTH-RO-02 astro summary | pass |
| AUTH-RO-03 questionnaire history | pass |
| AUTH-RO-04 notifications unread-count | pass |
| 是否发现隐私字段泄露 | no |
| 是否写 DB | no |
| 是否修改 runtime | no |
| 是否仍禁止 write smoke | yes |
| 是否仍禁止 v2 skeleton | yes |

## 8. 下一步建议

1. 提交本阶段 authenticated readonly smoke Batch 2 rerun 报告。
2. write smoke 继续禁止；如需执行任何 POST，必须另行授权 smoke 账号、cleanup / recovery 与停止条件。
3. conditional / side-effect smoke 继续单独授权：astro chart、messages、media、RTC LiveKit 仍不得默认执行。
4. v2 skeleton runtime 继续禁止，直到完整 v1 P0 smoke 与 Gate P1 carry-over 获得 GPT 顾问确认。
