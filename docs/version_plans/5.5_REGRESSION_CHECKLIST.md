# EliteSync 5.5 Regression Checklist

Updated: 2026-05-08

Version under check: `5.5` real small-sample feedback absorption.

Current baseline:

- Latest completed version: `5.4`
- 5.4 status: `pass with observations`
- Current release baseline: `0.05.04 / 50400`
- 5.5 runtime implementation: not started

## 1. Startup Gates

- [x] Feedback matrix contains at least one concrete real feedback row before runtime implementation.
- [x] Each P0 / P1 item has page / path / evidence or user-provided feedback text.
- [ ] `services/backend-laravel/config/matching.php` remains excluded unless user confirms matching marker semantics.
- [x] No release version bump is included in 5.5 runtime slices.
- [x] No database schema, migration, backup, restore, delete, truncate, or production rewrite operation is included.

## 2. Protected Surface Regression

When 5.5 runtime changes begin, verify the affected surfaces and these core routes:

- [x] Home remains reachable.
- [x] Discover remains reachable.
- [x] Match remains reachable.
- [x] Messages remains reachable.
- [x] Chat room remains reachable.
- [x] Notification Center / Messages notification surface remains reachable in logged-in smoke.
- [x] Me / Profile remains reachable.
- [x] Edit Profile remains reachable in ADB direct-route smoke.
- [x] Settings remains reachable from Profile.
- [x] Version Center remains reachable and reports service version status in logged-in smoke.
- [x] RTC permission / call surfaces remain reachable if touched; Claude logged-in smoke reached the voice-call page. This is not LiveKit media success evidence.

## 3. Contract Boundaries

- [ ] Profile truth chain is not modified.
- [ ] Messages API contract is not modified.
- [ ] Media upload / content contract is not modified.
- [ ] Notification payload contract is not modified.
- [ ] RTC / LiveKit contract is not modified.
- [ ] Version check contract is not modified.
- [ ] 5.4 Admin Dashboard remains read-only.

## 4. Observation Boundaries

These must not be marked passed without current approved evidence:

- [ ] Cloud DB read-only audit.
- [ ] Backup existence.
- [ ] Restore drill.
- [ ] Migration-level checks.
- [ ] Queue / logs.
- [x] RTC success evidence for current debug-build app-to-app LiveKit call `115`; backend deployment parity remains separate.
- [ ] Matching algo marker semantics.

## 5. Evidence Requirements

- [x] Current evidence comes from `0.05.04 / 50400` emulator checks or user-supplied current-session screenshots.
- [x] Screenshot / XML / Appium page-source references are recorded in `5.5_FEEDBACK_EVIDENCE_INDEX.md`.
- [x] Feedback rows link to evidence IDs or current-session user screenshots.
- [x] Cross-layer issues have not been patched blindly; `5.5-FB-002` remains data-dependent until active match data exists.

## 6. Current 5.5 Runtime Slice Checks

- [x] `5.5-FB-001` Status Plaza response parsing compatibility: focused tests passed; Claude UI precheck passed; user accepted.
- [x] `5.5-FB-003` Profile floating publish action: Claude confirmed before fix and verified after fix.
- [x] `5.5-FB-004` Astro Overview Ziwei preview: layout fixed; Claude structural smoke passed.
- [x] `5.5-FB-005` Status Author Profile parsing compatibility: focused tests passed; Claude verified after fix.
- [x] `5.5-FB-006` RTC receiver-side incoming-call watcher: focused Flutter analyze/tests and backend `RtcApiTest` passed; phone receiver reached `语音通话` / `in_call` for API-assisted call `114`.
- [ ] `5.5-FB-002` Match Detail success state: requires active match data / Drop-unlocked state retest.
- [x] Claude exploratory smoke covered 12 surfaces with 9 pass, 0 fail, and 3 uncertain direct-route checks.
- [x] Auth-gated follow-up smoke closed for Notification Center / Messages notification surface, Version Center, and RTC call reachability using user-approved smoke-account login.
- [x] RTC dual-device baseline devices are ready: emulator and phone both run `0.05.04 / 50400`.
- [x] RTC receiver-side incoming-call UI and accept/connect path verified by API-assisted call `114` on the phone.
- [x] Full RTC dual-device media success verified on current debug build with app-to-app call `115`: emulator caller and phone receiver reached created / accepted / connected / heartbeat / ended, both sides emitted LiveKit local and remote audio evidence, and the user audibly confirmed phone-side audio capture.
- [x] Aliyun backend deploy parity verified for the RTC 30s invite timeout by remote file grep, service status, and `/up` health check. No remote DB write-style create/end call verification was performed.
