# EliteSync 6.0-A1 Authenticated Readonly Smoke Batch 2 Report

执行时间：2026-05-14 15:41:46 +08:00  
当前阶段：A1 authenticated readonly smoke execution - Batch 2  
授权类型：production-readonly-explicit + authenticated readonly only  
BASE_URL：`http://101.133.161.203`  
报告定位：authenticated readonly smoke Batch 2 执行报告。本轮因 `$env:ELITESYNC_TOKEN_A` 未临时注入，按 preflight 停止条件阻断，未请求 API。

## 0. 执行边界

- 本轮原授权只允许执行以下 4 个 authenticated readonly GET：
  - `GET /api/v1/profile/basic`
  - `GET /api/v1/profile/astro/summary`
  - `GET /api/v1/questionnaire/history`
  - `GET /api/v1/notifications/unread-count`
- 本轮未执行任何 POST。
- 本轮未执行 `GET /api/v1/profile/astro/chart`。
- 本轮未执行 `GET /api/v1/messages`。
- 本轮未执行 `GET /api/v1/media/{assetId}`。
- 本轮未执行 `GET /api/v1/rtc/calls/{callId}/livekit`。
- 本轮未获取、生成、保存或回显 token。
- 本轮未使用真实用户账号。
- 本轮未写 DB，未执行 migration，未修改 production 配置。
- 本轮未修改 `apps/`、`services/`、`database/`、`scripts/` 下任何 runtime 或 release 文件。
- 本轮未运行 release script，未生成 APK，未进入 v2 skeleton runtime。

## 1. Git 与环境状态

| 项 | 结果 |
| --- | --- |
| 当前分支 | `feature/5.0-alpha-readiness-20260501` |
| 当前 HEAD | `fcf75f23` |
| git status 执行前 | clean |
| git status 执行后 | 仅新增本报告文件 |
| BASE_URL | `http://101.133.161.203` |
| 授权类型 | `production-readonly-explicit + authenticated readonly only` |

## 2. Token 处理结果

| 项 | 结果 |
| --- | --- |
| token 是否请求 | no |
| token 是否生成 | no |
| token 是否保存 | no |
| token 是否回显 | no |
| token 是否写入文件 | no |
| token 环境变量状态 | `$env:ELITESYNC_TOKEN_A` 不存在或为空 |
| 停止原因 | token 未临时注入，按授权计划和 preflight 停止条件阻断 |

## 3. Smoke 结果总表

| Smoke ID | 名称 | Endpoint | 结果 | HTTP status | 是否 auth required | 是否写 DB | 响应字段摘要 | Observation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AUTH-RO-01 | profile basic GET | `/api/v1/profile/basic` | blocked | n/a | yes | no | 未请求 API，无响应 | token 未临时注入 |
| AUTH-RO-02 | astro summary GET | `/api/v1/profile/astro/summary` | blocked | n/a | yes | no | 未请求 API，无响应 | token 未临时注入 |
| AUTH-RO-03 | questionnaire history GET | `/api/v1/questionnaire/history` | blocked | n/a | yes | no | 未请求 API，无响应 | token 未临时注入 |
| AUTH-RO-04 | notifications unread-count GET | `/api/v1/notifications/unread-count` | blocked | n/a | yes | no | 未请求 API，无响应 | token 未临时注入 |

## 4. 响应字段摘要

本轮没有请求任何 authenticated API，因此没有响应字段可摘要。

隐私与脱敏状态：

- 是否发现 token 泄露：no。
- 是否发现手机号泄露：no。
- 是否发现密码泄露：no。
- 是否发现私聊正文泄露：no。
- 是否发现完整出生时间泄露：no。
- 是否发现精确经纬度泄露：no。
- 是否发现 DB host / Redis host / OSS key / SSH key / `.env` secret 泄露：no。
- 是否发现 LiveKit token / secret 泄露：no。
- 是否发现真实用户身份信息泄露：no。

## 5. 保护面确认

| 项 | 结果 |
| --- | --- |
| 是否请求 API | no |
| 是否请求 token | no |
| 是否回显 token | no |
| 是否写 DB | no |
| 是否执行 migration | no |
| 是否修改 runtime | no |
| 是否修改 `.env` | no |
| 是否修改 release chain | no |
| 是否运行 release script | no |
| 是否生成 APK | no |
| 是否进入 v2 skeleton | no |

## 6. 判定

| 项 | 判定 |
| --- | --- |
| AUTH-RO-01 profile basic | blocked |
| AUTH-RO-02 astro summary | blocked |
| AUTH-RO-03 questionnaire history | blocked |
| AUTH-RO-04 notifications unread-count | blocked |
| 是否发现隐私字段泄露 | no |
| 是否写 DB | no |
| 是否修改 runtime | no |
| 是否仍禁止 write smoke | yes |
| 是否仍禁止 v2 skeleton | yes |

## 7. 下一步建议

1. 如需继续 Batch 2 authenticated readonly smoke，用户需在执行当次将 token 临时注入当前 PowerShell session，例如 `$env:ELITESYNC_TOKEN_A = "<TOKEN_FROM_USER_AT_RUNTIME>"`。
2. token 仍不得写入 Markdown、日志、截图或 git，Codex 不得回显 token。
3. token 注入后，下一轮仍只能执行用户授权的 4 个 authenticated readonly GET。
4. write smoke 继续禁止。
5. v2 skeleton runtime 继续禁止。
